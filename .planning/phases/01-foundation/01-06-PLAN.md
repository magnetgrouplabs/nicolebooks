---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on: [01-03, 01-05]
files_modified:
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/components/Header.tsx
  - src/renderer/src/components/Sidebar.tsx
  - src/renderer/src/components/EmptyState.tsx
  - src/renderer/src/components/HealthIndicator.tsx
  - src/renderer/src/screens/BillsScreen.tsx
  - src/renderer/src/screens/HistoryScreen.tsx
  - src/renderer/src/screens/SettingsScreen.tsx
autonomous: true
requirements: [BRAND-01, BRAND-02, PLAT-02]
must_haves:
  truths:
    - "The window shows a branded header with the plain NicoleBooks wordmark and no logo"
    - "A 280px left sidebar lists Bills, History, and Settings; the active item shows violet text and a violet left-edge bar"
    - "The content region swaps between Bills, History, and Settings placeholder screens"
    - "The Settings screen shows Secret store: OK from a real renderer-to-IPC-to-main-to-keychain round trip"
    - "The window follows the OS light/dark preference with no theme flash"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "App shell: CSS grid header 56px, sidebar 280px, swappable content region"
      min_lines: 30
    - path: "src/renderer/src/components/Header.tsx"
      provides: "Branded header with wordmark and neutral connection-status slot"
      contains: "NicoleBooks"
    - path: "src/renderer/src/components/HealthIndicator.tsx"
      provides: "Secret store health indicator driving the SC2 plus SC4 round trip on mount"
      contains: "window.api.secrets"
    - path: "src/renderer/src/components/Sidebar.tsx"
      provides: "280px nav with Bills, History, Settings and active-state styling"
      contains: "280"
  key_links:
    - from: "src/renderer/src/components/HealthIndicator.tsx"
      to: "window.api.secrets"
      via: "set then get canary on mount"
      pattern: "window.api.secrets"
    - from: "src/renderer/src/main.tsx"
      to: "window.api.theme"
      via: "dark class mirror on boot and onChange"
      pattern: "window.api.theme"
---

<objective>
Build the user-visible Walking Skeleton: the branded app shell (56px header with the NicoleBooks wordmark and neutral connection slot, 280px sidebar with Bills/History/Settings, swappable content region), the three placeholder screens, and the Settings "Secret store" health indicator that performs the real secret round trip on mount. Mirror the OS color scheme onto the renderer with no flash. After this plan, a user can launch the app and see "Secret store: OK".

Purpose: This is where every seam becomes a working, branded product frame. The health indicator is the permanent SC2 plus SC4 proof surface (D-11) that Phases 3 and 4 build their real settings onto.
Output: A launchable, branded, navigable shell whose Settings screen proves the keychain round trip end to end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
Consumed contracts (from prior plans - use verbatim):
- window.api.secrets.set/get/delete and window.api.theme.get/onChange (preload from 01-02; live handlers from 01-05)
- src/renderer/src/globals.css theme classes and src/renderer/src/lib/utils.ts cn() (01-03)
- src/renderer/src/components/ui/{button,badge,separator,tooltip}.tsx shadcn primitives (01-03)
- lucide-react icons: Receipt, History, Settings, ShieldCheck, ShieldAlert, Circle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell frame, header, sidebar, empty state, and OS theme mirror</name>
  <files>src/renderer/src/App.tsx, src/renderer/src/main.tsx, src/renderer/src/components/Header.tsx, src/renderer/src/components/Sidebar.tsx, src/renderer/src/components/EmptyState.tsx</files>
  <read_first>
    - 01-UI-SPEC.md Layout Frame (lines 42-56): grid rows 56px 1fr, cols 280px 1fr, header spans both columns at z-index 20, structural radius 0, content padding 24px, default active destination Bills
    - 01-UI-SPEC.md Typography (lines 81-91): wordmark Jost 20px 600, empty-state heading Jost 24px 600, body DM Sans 16px 400, nav label DM Sans 14px (600 when active)
    - 01-UI-SPEC.md Color (lines 96-138) and Motion (lines 156-164): active nav violet text plus violet left-edge bar plus focus ring; hover tint rgba(108,0,255,0.06); 150ms fast; connection slot neutral #6e6e73
    - 01-UI-SPEC.md Copywriting Contract (lines 168-193): every string verbatim (wordmark NicoleBooks, Not connected, empty-state copy, nav labels)
    - 01-PATTERNS.md net-new components section (lines 262-269) and main.tsx section (lines 233-239): the dark-class mirror is net-new
    - 01-RESEARCH.md Pitfall 4 (lines 516-520): read theme in main, toggle documentElement.classList synchronously before first paint, keep show:false until ready, subscribe to onChange to avoid FOUC
    - src/renderer/src/main.tsx current state (from 01-01) and globals.css (01-03)
  </read_first>
  <action>
    Author src/renderer/src/App.tsx as the shell using a CSS grid: `grid-template-rows: 56px 1fr; grid-template-columns: 280px 1fr;` with Header spanning both columns (z-index 20, structural radius 0), Sidebar in the left column below the header, and a content region (24px padding, dominant surface background) that renders the active screen. Hold active-destination state in App (default Bills, D-09) and pass it plus a setter to Sidebar. Author Header.tsx: left side the wordmark NicoleBooks (Jost 20px 600, plain text, no image, no logo, BRAND-02), right side the connection-status slot as a neutral filled Circle dot in #6e6e73 plus the label Not connected (using the shadcn badge, UI-SPEC line 185). Author Sidebar.tsx: 280px wide, three nav items (Receipt/Bills, History/History, Settings/Settings) each min-height 40px, icon 20px plus 14px label with 12px gap and 12px horizontal padding; the active item gets violet text (text-primary) plus a violet left-edge indicator bar and semibold weight; hover uses the rgba(108,0,255,0.06) tint at 150ms; focus-visible uses the ring token. Author EmptyState.tsx: centered heading (Jost 24px 600) plus body (DM Sans 16px 400) with a space.16 top offset, taking heading and body as props. Modify src/renderer/src/main.tsx to import globals.css and render App, apply `font-sans antialiased` on the root, and BEFORE first meaningful paint call `window.api.theme.get()` and `document.documentElement.classList.toggle('dark', isDark)` synchronously, then subscribe with `window.api.theme.onChange` to re-toggle on OS change (Pitfall 4). Use only semantic Tailwind classes that resolve to the theme variables; never hardcode hex in components. No em or en dashes in any copy or code.
  </action>
  <acceptance_criteria>
    - App.tsx grid uses a 56px header row and a 280px sidebar column, header spanning both columns
    - Header.tsx renders the literal text NicoleBooks and contains no img, svg logo, or image asset for a logo (BRAND-02)
    - Sidebar.tsx renders exactly three items labeled Bills, History, Settings, defaults active to Bills, and applies text-primary plus a left-edge indicator to the active item
    - main.tsx toggles the `dark` class from window.api.theme.get() before first paint and subscribes to onChange
    - No component hardcodes a hex color; all colors come from semantic theme classes
    - No em or en dash characters appear in any authored file; all copy matches the UI-SPEC Copywriting Contract verbatim
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('src/renderer/src/components/Header.tsx','utf8');const m=fs.readFileSync('src/renderer/src/main.tsx','utf8');const a=fs.readFileSync('src/renderer/src/App.tsx','utf8');const ok=/NicoleBooks/.test(h)&&!/<img/i.test(h)&&!/import\s+[^;]*\.(png|jpe?g|svg|gif|webp)/i.test(h)&&/window\.api\.theme/.test(m)&&/280/.test(a)&&![h,m,a].some(s=>/[\u2013\u2014]/.test(s));process.exit(ok?0:1)" && echo SHELL_OK</automated>
  </verify>
  <done>The branded, navigable shell launches with the wordmark, 280px sidebar, swappable content region, and correct OS-driven light/dark theme with no flash.</done>
</task>

<task type="auto">
  <name>Task 2: Placeholder screens and the Secret store health indicator round trip</name>
  <files>src/renderer/src/screens/BillsScreen.tsx, src/renderer/src/screens/HistoryScreen.tsx, src/renderer/src/screens/SettingsScreen.tsx, src/renderer/src/components/HealthIndicator.tsx</files>
  <read_first>
    - 01-UI-SPEC.md Per-screen copy (lines 183-190): exact headings and bodies for Bills, History, and Settings, and the health indicator states
    - 01-UI-SPEC.md Copywriting Contract error state (lines 176-178): the exact Secret store unavailable supporting copy
    - 01-UI-SPEC.md Color status colors (lines 128-138): success #28a745 for OK, destructive #ff3b30 for unavailable, via semantic classes
    - 01-CONTEXT.md D-11: prove the round trip by storing then reading a canary through the real renderer-to-IPC-to-main-to-keychain path; permanent, not throwaway
    - 01-PATTERNS.md net-new components note on HealthIndicator (line 269) and RESEARCH diagram (lines 176-220)
    - window.api.secrets from the preload (01-02) and live handlers (01-05)
  </read_first>
  <action>
    Author BillsScreen.tsx and HistoryScreen.tsx to render EmptyState with the verbatim UI-SPEC copy: Bills heading `No bills to review` and body `Bills you import will appear here for review before they post to QuickBooks.`; History heading `No history yet` and body `Batches you send to QuickBooks will be listed here, along with the entries in each.` (Receipt and History lucide icons respectively). Author HealthIndicator.tsx as the permanent SC2 plus SC4 proof: inside a shadcn card, on mount run an effect that calls `window.api.secrets.set('canary','ok')` then `window.api.secrets.get('canary')`; if the returned value equals `ok`, render the healthy state (ShieldCheck plus success #28a745 via semantic class) with label `Secret store: OK` and supporting line `Your machine's secure keychain is available and working.`; on any failure or a non-ok result render the unavailable state (ShieldAlert plus destructive class) with label `Secret store: unavailable` and the exact error supporting copy from UI-SPEC lines 176-178. Author SettingsScreen.tsx hosting the section label `Secret store`, the HealthIndicator, and the muted note `Connection and model settings will appear here in a later update.`. Never render or log the secret value beyond the ok/unavailable state. All copy verbatim from the contract; no personal names; no em or en dashes.
  </action>
  <acceptance_criteria>
    - BillsScreen and HistoryScreen render the exact UI-SPEC headings and bodies verbatim
    - HealthIndicator.tsx calls window.api.secrets.set then window.api.secrets.get on mount and renders OK only when the round trip returns the canary value
    - The unavailable branch renders the exact UI-SPEC error supporting copy
    - SettingsScreen shows the Secret store section, the health indicator, and the muted later-update note
    - Status colors come from semantic classes (success/destructive), not hardcoded hex, and the copy contains no em or en dashes
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const hi=fs.readFileSync('src/renderer/src/components/HealthIndicator.tsx','utf8');const b=fs.readFileSync('src/renderer/src/screens/BillsScreen.tsx','utf8');const ok=/window\.api\.secrets\.set/.test(hi)&&/window\.api\.secrets\.get/.test(hi)&&/No bills to review/.test(b)&&![hi,b].some(s=>/[\u2013\u2014]/.test(s));process.exit(ok?0:1)" && echo SCREENS_OK</automated>
  </verify>
  <done>The three screens render with locked copy, and the Settings health indicator proves the secret round trip live, showing Secret store: OK when the keychain path works end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI to window.api | The UI reaches privileged capability only through the allow-listed window.api; the health indicator exercises exactly this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Elevation of Privilege | renderer components | mitigate | Components call only window.api named methods; no Node, fs, db, or ipcRenderer access exists in the sandboxed renderer (enforced by 01-01 posture and 01-02 bridge) |
| T-01-05 | Information Disclosure | HealthIndicator | mitigate | Only a non-sensitive canary flows through; the UI renders an ok/unavailable state, never the raw secret, and logs no secret value (Task 2) |
| T-01-11 | Spoofing | branding/wordmark | accept | Wordmark is a static product name NicoleBooks with no logo and no personal names; no spoofable identity claim (Task 1) |
</threat_model>

<verification>
- `npm run build` succeeds and `npm run dev` launches the full branded shell
- The Settings screen shows Secret store: OK on a working machine
- Nav switches between the three screens; active-state styling applies
- The window follows the OS light/dark setting with no flash
- No component hardcodes hex; no copy contains em or en dashes
</verification>

<success_criteria>
A user launches NicoleBooks and sees the branded two-process shell (wordmark, 280px sidebar, themed to the Magnet Group tokens in OS light or dark), navigates the three placeholder screens, and sees Secret store: OK on Settings from a real end-to-end round trip. (Completes SC1 branded window, BRAND-01, BRAND-02, and the user-visible SC2 plus SC4 proof.)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done. Record how the health indicator surfaces the round-trip result and confirm the wordmark-only, no-logo header, so 01-07 e2e specs can assert against the real DOM.
</output>
</output>
