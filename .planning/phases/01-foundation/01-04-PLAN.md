---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/main/db/connection.ts
  - src/main/db/migrate.ts
  - src/main/db/migrations/0001_init.ts
  - src/main/secrets/secret-store.ts
  - test/migrate.test.ts
  - test/secret-store.test.ts
  - test/no-secret-leak.test.ts
autonomous: true
requirements: [PLAT-01, PLAT-02]
must_haves:
  truths:
    - "A SQLite database is created at userData/app.db on first open"
    - "A forward-only migration runner applies migration 0001 and bumps user_version from 0 to 1"
    - "The app_settings table exists after migration and re-running the migrator is idempotent"
    - "safeStorage encrypts a value to userData/secrets.enc with owner-only permissions and never touches SQLite"
  artifacts:
    - path: "src/main/db/migrate.ts"
      provides: "Forward-only user_version migration runner wrapped in a transaction"
      contains: "user_version"
    - path: "src/main/db/migrations/0001_init.ts"
      provides: "Migration 0001 creating app_settings (key TEXT PRIMARY KEY, value TEXT NOT NULL) STRICT"
      contains: "app_settings"
    - path: "src/main/secrets/secret-store.ts"
      provides: "safeStorage-backed set/get/delete writing base64 ciphertext to secrets.enc"
      contains: "safeStorage"
    - path: "test/no-secret-leak.test.ts"
      provides: "Assertion that no secret material appears in the DB file or logs"
      min_lines: 15
  key_links:
    - from: "src/main/db/connection.ts"
      to: "app.getPath('userData')/app.db"
      via: "better-sqlite3 open path"
      pattern: "userData"
    - from: "src/main/secrets/secret-store.ts"
      to: "userData/secrets.enc"
      via: "writeFileSync mode 0o600"
      pattern: "0o600"
---

<objective>
Build the two privileged main-process services the whole app depends on, with their unit tests: the SQLite connection plus a forward-only `user_version` migration runner that creates the minimal `app_settings` schema (SC3), and the `safeStorage` secret store that writes ciphertext only to `secrets.enc` and never to SQLite (SC2). No IPC wiring here (that is 01-05); these are pure, unit-testable services.

Purpose: These are the persistence and secret seams every later phase reuses (Phase 3 AI key, Phase 4 QuickBooks tokens, Phases 2/3/7 feature tables). Getting the migration idempotency and the secret isolation right here is load-bearing.
Output: A migrating SQLite layer, a safeStorage secret store, and unit tests proving migration idempotency, the encryption round trip, and no secret leakage into the DB or logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLite connection and forward-only migration runner</name>
  <files>src/main/db/connection.ts, src/main/db/migrate.ts, src/main/db/migrations/0001_init.ts, test/migrate.test.ts</files>
  <behavior>
    - Fresh DB reports user_version 0; after migrate() it reports user_version 1
    - After migrate() the app_settings table exists with columns key and value
    - Running migrate() a second time is a no-op (idempotent) and does not throw
    - A value inserted into app_settings survives closing and reopening the same DB file
  </behavior>
  <read_first>
    - 01-RESEARCH.md Pattern 6 (lines 431-470): the exact forward-only user_version migration runner and the 0001 up() creating app_settings STRICT
    - 01-RESEARCH.md app_settings read/write example (lines 567-586): the prepared-statement UPSERT shape (used by 01-05, referenced here for the table shape)
    - 01-RESEARCH.md Anti-Patterns (lines 476-477): open the DB at app.getPath('userData'), never a relative path or the bundle; never write secrets into SQLite
    - 01-CONTEXT.md D-13, D-14, D-15: minimal schema only (schema-version plus app_settings), demonstrate persistence via app_settings, NO feature tables
    - 01-PATTERNS.md blueprint row for migrate.ts and 0001_init.ts (line 256) and the "No Analog Found" connection.ts row (line 309)
  </read_first>
  <action>
    Author src/main/db/connection.ts to open a better-sqlite3 database at `join(app.getPath('userData'), 'app.db')`, enable WAL journal mode, and export the db handle plus a lazy opener so unit tests can point at a temp path. Author src/main/db/migrate.ts per Pattern 6: read `PRAGMA user_version`, filter the code-defined migration list to versions greater than current, run them in a `db.transaction`, and after each `up()` bump `db.pragma('user_version = N')` where N is the integer version from the code-controlled list only (never renderer input; this is the sole interpolated SQL, T-01-06). Author src/main/db/migrations/0001_init.ts exporting `{ version: 1, up }` where up runs `CREATE TABLE IF NOT EXISTS app_settings (key TEXT PRIMARY KEY, value TEXT NOT NULL) STRICT`. Create NO feature tables (D-15). Write test/migrate.test.ts covering the four behaviors above against a temp-file DB (open, assert user_version 0, migrate, assert user_version 1 and app_settings exists, migrate again and assert no-op, insert-close-reopen-read to prove persistence). No em or en dashes.
  </action>
  <acceptance_criteria>
    - `npx vitest run test/migrate.test.ts` exits 0 with all four behavior tests passing
    - migrate.ts interpolates SQL only in the `PRAGMA user_version = N` statement and uses the code-defined version integer
    - 0001_init.ts creates app_settings with `key TEXT PRIMARY KEY, value TEXT NOT NULL` and STRICT, and no other tables
    - connection.ts opens the DB under `app.getPath('userData')` (source assertion), not a relative path
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/migrate.test.ts</automated>
  </verify>
  <done>The DB is created and migrated forward-only on first run, app_settings exists, migration is idempotent, and data survives a reopen. SC3 unit coverage is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: safeStorage secret store and secret-isolation tests</name>
  <files>src/main/secrets/secret-store.ts, test/secret-store.test.ts, test/no-secret-leak.test.ts</files>
  <behavior>
    - set(key,value) then get(key) returns the original value (round trip through safeStorage)
    - get on an unknown key returns null
    - delete(key) removes the entry so a subsequent get returns null
    - when safeStorage.isEncryptionAvailable() is false, set throws SECRET_STORE_UNAVAILABLE
    - the secrets.enc file holds only base64 ciphertext, and neither app.db nor any log line contains the plaintext value
  </behavior>
  <read_first>
    - 01-RESEARCH.md Pattern 3 (lines 368-411): the exact secret-store implementation (isEncryptionAvailable guard, base64 ciphertext map, writeFileSync mode 0o600, never SQLite)
    - 01-RESEARCH.md Pitfall 3 (lines 510-514): safeStorage must be used after app ready; handle the unavailable branch gracefully
    - 01-CONTEXT.md D-10, D-11, D-12: safeStorage keychain service; canary round trip; ciphertext in secrets.enc, never in the DB
    - 01-VALIDATION.md rows SC2-roundtrip and SC2-noleak (lines 45-46) and Manual-Only keychain note (line 75)
    - 01-PATTERNS.md blueprint row for secret-store.ts (line 255) and Shared Pattern "safeStorage-only secrets" (lines 287-289)
  </read_first>
  <action>
    Author src/main/secrets/secret-store.ts per Pattern 3: a FILE() helper returning `join(app.getPath('userData'), 'secrets.enc')`; readAll/writeAll functions serializing a `Record<string,string>` of base64 ciphertext (writeAll uses `{ mode: 0o600 }`, owner-only, T-01-04); `available()` returns `safeStorage.isEncryptionAvailable()`; `set(key,value)` throws `SECRET_STORE_UNAVAILABLE` when encryption is unavailable, else stores `safeStorage.encryptString(value).toString('base64')`; `get(key)` decrypts or returns null; `delete(key)` removes the key. Never read or write any secret material to SQLite or to a log line (T-01-04, T-01-05, D-12). Write test/secret-store.test.ts with safeStorage mocked (encryptString/decryptString reversible stub, isEncryptionAvailable toggle) and a temp userData path, covering the round trip, unknown-key null, delete, and the unavailable-throws branch. Write test/no-secret-leak.test.ts that: sets a known canary value through the store, then reads the raw secrets.enc bytes and asserts the plaintext canary is NOT present (only base64 ciphertext), reads the app.db file bytes and asserts the canary is NOT present, and captures a log buffer and asserts the canary plaintext never appears in it. No dashes anywhere.
  </action>
  <acceptance_criteria>
    - `npx vitest run test/secret-store.test.ts` exits 0 with the round trip, null, delete, and unavailable-throws cases passing
    - `npx vitest run test/no-secret-leak.test.ts` exits 0: the plaintext canary is absent from secrets.enc, absent from app.db, and absent from captured logs
    - secret-store.ts writes secrets.enc with `{ mode: 0o600 }` (source assertion) and contains no `better-sqlite3` or `app.db` reference
    - secret-store.ts throws `SECRET_STORE_UNAVAILABLE` when `isEncryptionAvailable()` is false
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/secret-store.test.ts test/no-secret-leak.test.ts</automated>
  </verify>
  <done>Secrets encrypt/decrypt through safeStorage into an owner-only secrets.enc file, the unavailable branch is handled, and tests prove no plaintext leaks into the DB or logs. SC2 unit coverage is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main process to local disk | The privileged main process owns all writes to app.db and secrets.enc under userData; the renderer never reaches these |
| secret material to persistence | A hard split: encrypted secret bytes go only to secrets.enc; app.db holds non-secret app_settings only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | secret-store secrets.enc | mitigate | safeStorage OS-backed encryption; base64 ciphertext only; writeFileSync mode 0o600 owner-only; never written to SQLite; secrets.enc gitignored in 01-01 (Task 2) |
| T-01-05 | Information Disclosure | secret-store logging | mitigate | Never log secret values; no-secret-leak.test asserts the canary plaintext is absent from logs (Task 2) |
| T-01-06 | Tampering | migrate PRAGMA user_version | mitigate | All app_settings SQL uses prepared statements; the only interpolated SQL is `PRAGMA user_version = N` with N a code-controlled integer, never renderer input (Task 1) |
| T-01-08 | Tampering | better-sqlite3 native module | mitigate | Uses the ABI-aligned better-sqlite3 rebuilt in 01-01; version pinned; slopcheck [OK] |
</threat_model>

<verification>
- `npx vitest run test/migrate.test.ts test/secret-store.test.ts test/no-secret-leak.test.ts` all pass
- migrate.ts uses prepared statements plus the single code-controlled PRAGMA interpolation
- secret-store.ts writes owner-only secrets.enc and never references SQLite
</verification>

<success_criteria>
SQLite is created and migrated forward-only with an idempotent runner and a minimal app_settings schema; the safeStorage secret store round-trips through secrets.enc with no leakage into the DB or logs. (Advances SC2 and SC3 at the service layer; IPC exposure lands in 01-05.)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done. Record the exported service function signatures (db handle, migrate(db), secretStore.set/get/delete/available) so 01-05 handlers import them verbatim, and note the secrets.enc and app.db on-disk locations.
</output>
