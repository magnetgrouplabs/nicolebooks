---
phase: 01-foundation
plan: 07
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - e2e/theme.spec.ts
  - e2e/secret-roundtrip.spec.ts
  - e2e/persistence.spec.ts
  - e2e/ipc-boundary.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [PLAT-01, PLAT-02]
must_haves:
  truths:
    - "An e2e test proves the secret round trip lands ciphertext in secrets.enc and nothing secret in app.db"
    - "An e2e test writes app_settings, relaunches the app, and reads the same value back"
    - "An e2e test proves the renderer cannot reach fs, db, keychain, or network except via window.api"
    - "CI runs install, native rebuild, and both suites on a windows and a macos runner"
  artifacts:
    - path: "e2e/secret-roundtrip.spec.ts"
      provides: "SC2 end-to-end keychain round trip and no-DB-leak assertion"
      min_lines: 20
    - path: "e2e/persistence.spec.ts"
      provides: "SC3 relaunch persistence proof"
      min_lines: 20
    - path: "e2e/ipc-boundary.spec.ts"
      provides: "SC4 renderer isolation proof"
      min_lines: 20
    - path: ".github/workflows/ci.yml"
      provides: "Matrix windows plus macos CI running rebuild and both test suites"
      contains: "matrix"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "electron-rebuild plus vitest plus playwright"
      via: "CI job steps"
      pattern: "electron-rebuild"
    - from: "e2e/secret-roundtrip.spec.ts"
      to: "secrets.enc under userData"
      via: "_electron.launch and file read"
      pattern: "secrets.enc"
---

<objective>
Complete the Wave 0 validation surface with the end-to-end Playwright `_electron` specs that prove the three remaining Success Criteria on the real running app (SC2 keychain round trip plus no-DB-leak, SC3 relaunch persistence, SC4 renderer isolation), and add the CI workflow that installs, rebuilds the native module, and runs both suites on a windows and a macos runner. The launch smoke (SC1) already exists from 01-01.

Purpose: These specs are the automated per-wave and phase-gate coverage; the CI matrix is the load-bearing PLAT-01 cross-OS gate that CAN be automated (native rebuild, keychain, persistence, isolation). What still needs a real machine is handled by the checkpoint in 01-08.
Output: A green end-to-end suite and a two-OS CI pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end specs for the secret round trip, persistence, and renderer isolation</name>
  <files>e2e/secret-roundtrip.spec.ts, e2e/persistence.spec.ts, e2e/ipc-boundary.spec.ts, e2e/theme.spec.ts</files>
  <read_first>
    - 01-VALIDATION.md Per-Task Verification Map (lines 41-49): SC2-roundtrip, SC2-noleak, SC3-persist, SC4-ipc, SC1-theme behaviors and commands
    - 01-RESEARCH.md Validation Architecture Phase Requirements to Test Map (lines 655-663): the e2e spec inventory and what each proves
    - 01-RESEARCH.md Validation Architecture what-is-CI-automatable note (line 681)
    - 01-RESEARCH.md system architecture diagram (lines 176-220): the round-trip data flow to assert against
    - playwright.config.ts and e2e/launch.spec.ts (from 01-01) for the _electron.launch harness pattern
    - 01-06 SUMMARY: how the health indicator and DOM expose the round-trip result
  </read_first>
  <action>
    Author e2e/secret-roundtrip.spec.ts: `_electron.launch` the built app, navigate to Settings, assert the health indicator shows Secret store: OK; then read the app userData path from the main process (via `app.getPath('userData')` through the Electron test API), read secrets.enc and assert it exists and contains base64 ciphertext (not the plaintext canary), and read app.db and assert the plaintext canary is absent (SC2 plus no-leak). Author e2e/persistence.spec.ts: launch, drive `window.api.settings.set('e2e_probe', <value>)` from the renderer, close the app, relaunch pointing at the same userData, and assert `window.api.settings.get('e2e_probe')` returns the same value (SC3 relaunch). Author e2e/ipc-boundary.spec.ts: launch, and in the renderer context assert that `window.require`, `window.process`, `window.module`, and a bare `ipcRenderer` are undefined, that `window.api` exposes only settings/secrets/theme, and that invoking a handler with a malformed payload (for example secrets.set with an over-long key) rejects (SC4 isolation plus Zod rejection). Author e2e/theme.spec.ts: launch and assert the documentElement carries or omits the `dark` class consistent with the OS/native theme value returned by window.api.theme.get() (SC1 theme). Use a fresh temp userData per test to keep runs isolated. No watch flags. No em or en dashes.
  </action>
  <acceptance_criteria>
    - `npx playwright test e2e/secret-roundtrip.spec.ts` passes: health shows OK, secrets.enc holds ciphertext, app.db has no plaintext canary
    - `npx playwright test e2e/persistence.spec.ts` passes: an app_settings value survives a relaunch
    - `npx playwright test e2e/ipc-boundary.spec.ts` passes: window.require/process/module and raw ipcRenderer are undefined, window.api exposes only the three groups, and a malformed payload rejects
    - `npx playwright test e2e/theme.spec.ts` passes: the dark class matches the native theme value
    - All four specs use isolated temp userData and no watch flags
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test e2e/secret-roundtrip.spec.ts e2e/persistence.spec.ts e2e/ipc-boundary.spec.ts e2e/theme.spec.ts</automated>
  </verify>
  <done>The full e2e suite proves SC2, SC3, and SC4 on the real running app, joining the SC1 launch and theme coverage.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow (matrix windows plus macos)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 01-VALIDATION.md Sampling Rate and Wave 0 Requirements (lines 28-67): the phase gate must be green on both Windows and Mac; CI installs, runs electron-rebuild, executes both suites
    - 01-RESEARCH.md Validation Architecture what-is-CI-automatable (lines 668, 681): both runners provide a keychain backend; native rebuild success must be proven on both OSes
    - 01-RESEARCH.md Environment Availability (lines 627-638): the rebuild toolchain fallbacks per OS
    - 01-PATTERNS.md Tests table CI row (line 98): matrix windows plus macos
  </read_first>
  <action>
    Author .github/workflows/ci.yml with a job matrix over `windows-latest` and `macos-latest`. Steps: checkout, setup-node at the RESEARCH Node floor (20.19+ or 22.12+), `npm ci` (which runs the postinstall electron-rebuild from 01-01), an explicit `npx electron-rebuild -f -w better-sqlite3` step so a rebuild failure is a distinct, visible CI failure (PLAT-01), `npm run build`, `npx vitest run`, install Playwright browsers if required, and `npx playwright test`. Do not embed any repository token or secret in the workflow (packaging and signing are Phase 8). No em or en dashes.
  </action>
  <acceptance_criteria>
    - ci.yml defines a matrix including windows-latest and macos-latest
    - ci.yml has a distinct electron-rebuild step, a build step, a vitest step, and a playwright step
    - ci.yml embeds no repository token, signing secret, or credential
    - The workflow is valid YAML (parses without error)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.github/workflows/ci.yml','utf8');const ok=/windows-latest/.test(s)&&/macos-latest/.test(s)&&/electron-rebuild/.test(s)&&/playwright/.test(s)&&!/[\u2013\u2014]/.test(s);process.exit(ok?0:1)" && echo CI_OK</automated>
  </verify>
  <done>CI installs, rebuilds the native module, and runs both suites on a windows and a macos runner, with no token embedded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner to repo | CI executes untrusted-at-scale build steps; it must not embed release tokens or signing secrets in Phase 1 (those are Phase 8) |
| test process to app under test | e2e tests observe the real IPC/DB/keychain boundary from outside; they assert the renderer isolation the app claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | ipc-boundary.spec assertions | mitigate | The e2e boundary spec proves malformed payloads reject and the renderer has no fs/db/keychain/net reach, validating the SC4 controls end to end (Task 1) |
| T-01-04 | Information Disclosure | secret-roundtrip.spec | mitigate | The e2e spec asserts ciphertext-only in secrets.enc and no plaintext canary in app.db, proving the no-leak control on the real app (Task 1) |
| T-01-12 | Information Disclosure | CI workflow | mitigate | No repository token or signing secret is embedded; update tokens and signing arrive only in Phase 8 (Task 2) |
| T-01-08 | Tampering | CI native rebuild | mitigate | An explicit electron-rebuild CI step surfaces an ABI or toolchain failure distinctly on both OSes (Task 2) |
</threat_model>

<verification>
- `npm run build && npx playwright test` runs all e2e specs green locally (Windows)
- `npx vitest run` remains green
- ci.yml parses as valid YAML and defines the windows plus macos matrix with a distinct rebuild step
- No token or secret is present in the workflow
</verification>

<success_criteria>
The end-to-end suite proves SC2, SC3, and SC4 on the real running app, and the CI matrix runs install, native rebuild, and both suites on windows-latest and macos-latest with no embedded secret. (Completes the automatable half of the PLAT-01 cross-OS gate; the real-machine half is the 01-08 checkpoint.)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-07-SUMMARY.md` when done. Record the exact full-suite command and any CI-only adjustments, so the 01-08 checkpoint reproduces the same commands on Anthony's real Mac and Windows machines.
</output>
