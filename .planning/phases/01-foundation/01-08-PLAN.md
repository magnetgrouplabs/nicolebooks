---
phase: 01-foundation
plan: 08
type: execute
wave: 6
depends_on: [01-07]
files_modified: []
autonomous: false
requirements: [PLAT-01, PLAT-02, BRAND-01, BRAND-02]
must_haves:
  truths:
    - "The app launches and the full suite passes on Anthony's real Mac, not only in CI"
    - "better-sqlite3 rebuilds and loads on the Mac with no NODE_MODULE_VERSION error"
    - "The keychain round trip shows Secret store: OK on a real Mac, and the unavailable copy is correct when the keychain is locked"
    - "The branded header, sidebar, and empty states match the UI-SPEC in light and dark on both OSes"
  artifacts: []
  key_links: []
---

<objective>
Close the load-bearing PLAT-01 cross-OS gate on real machines. CI (01-07) covers the automatable half; three things need a human on a real Mac and a real Windows machine: the native `better-sqlite3` rebuild (prebuild availability for Electron 43 is unverified and the local Windows toolchain is incomplete), the keychain round trip and its unavailable branch (a locked login keychain is the realistic failure case CI cannot reproduce), and visual brand fidelity to the Magnet Group tokens in light and dark.

Purpose: This is the phase acceptance gate. Everything automatable is already proven; this checkpoint confirms the Walking Skeleton actually launches, secures, persists, and looks right on the two shipping OSes before the phase is declared done.
Output: Human confirmation (or a defect list) for the three manual-only verifications from VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cross-OS real-machine verification (native rebuild, keychain, visual fidelity)</name>
  <files>none (verification only; no files modified)</files>
  <read_first>
    - 01-VALIDATION.md Manual-Only Verifications (lines 71-77): the three real-machine checks and why each cannot be fully automated
    - 01-UI-SPEC.md Color, Typography, and Copywriting Contract: the visual fidelity reference for both palettes
    - 01-01 SUMMARY and 01-07 SUMMARY: the rebuild path taken on Windows and the exact full-suite command to reproduce on the Mac
  </read_first>
  <action>
    Pause for a human to run the three manual-only verifications from 01-VALIDATION.md on Anthony's real Mac and re-confirm on the real Windows machine: (1) the better-sqlite3 native rebuild and load, (2) the keychain OK and locked-keychain-unavailable paths, and (3) visual brand fidelity to the UI-SPEC in light and dark. All automatable coverage already passed in 01-07 and CI; this task collects human confirmation or a defect list. Do not modify code in this task; defects route to a gap-closure plan.
  </action>
  <what-built>
    A branded, hardened Electron Walking Skeleton with a typed IPC boundary, SQLite persistence with migrations, and a safeStorage secret store. CI already runs the full Vitest plus Playwright suite on windows-latest and macos-latest. This checkpoint confirms the three manual-only verifications from 01-VALIDATION.md (lines 71-77) on real hardware.
  </what-built>
  <how-to-verify>
    On Anthony's real Mac (and re-confirm on the real Windows machine):
    1. Native rebuild: from a clean clone run `npm ci` then `npx electron-rebuild -f -w better-sqlite3`. Expect exit 0 and no NODE_MODULE_VERSION error. On macOS, if a source compile is triggered, run `xcode-select --install` first. Record whether a prebuild was downloaded or a source compile ran.
    2. Launch and full suite: run `npm run build` then `npx vitest run && npx playwright test`. Expect all green.
    3. Keychain OK path: `npm run dev`, open Settings, confirm the indicator reads `Secret store: OK` with the supporting line about the secure keychain being available.
    4. Keychain unavailable path (Mac): lock or temporarily remove the login keychain, relaunch, and confirm the app renders the exact `Secret store: unavailable` copy from the UI-SPEC gracefully (no crash, no stack trace).
    5. Visual fidelity: on both OSes, in OS light mode and OS dark mode, compare the header (NicoleBooks wordmark, no logo), the 280px sidebar (Bills/History/Settings, active violet text plus left-edge bar), and the Bills and History empty states against 01-UI-SPEC.md. Confirm foreground reads #343434 in light, the dark palette matches, and there is no theme flash on launch.
  </how-to-verify>
  <verify>
    <human-check>All five verification steps pass on both a real Mac and a real Windows machine, in light and dark, with no defects against the UI-SPEC.</human-check>
  </verify>
  <resume-signal>Type "approved" if all three verifications pass on both OSes, or list the specific defects (which OS, which check, what differed from the UI-SPEC) so a gap-closure plan can address them.</resume-signal>
  <done>A human has confirmed the native rebuild, keychain OK and unavailable paths, and visual fidelity on both shipping OSes, or filed a defect list for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real OS keychain to app | Only a real machine exercises the true macOS Keychain and Windows DPAPI backends, including the locked-keychain failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | real keychain round trip | mitigate | Human confirms the OK path stores via the real OS keychain and the unavailable branch degrades gracefully with the locked UI-SPEC copy, not a leak or crash (Task 1) |
| T-01-08 | Tampering | native rebuild on real Mac | mitigate | Human confirms better-sqlite3 loads against Electron 43 on real hardware, closing the prebuild-availability open question for both OSes (Task 1) |
</threat_model>

<verification>
- Native rebuild exits 0 and better-sqlite3 loads on the real Mac and real Windows machine
- The full Vitest plus Playwright suite is green on both real machines
- Settings shows Secret store: OK on the happy path and the exact unavailable copy when the keychain is locked
- Header, sidebar, and empty states match the UI-SPEC in light and dark on both OSes
</verification>

<success_criteria>
All three manual-only verifications pass on both shipping OSes: the native module rebuilds and loads, the keychain round trip works and fails gracefully, and the brand fidelity matches the UI-SPEC in both palettes. (Closes the real-machine half of the PLAT-01 gate and the SC1/SC2 human confirmation; the phase is ready for /gsd:verify-work.)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-08-SUMMARY.md` when done. Record the rebuild path taken on each OS (prebuild vs source compile plus any toolchain installed), the keychain result on both paths, and any visual-fidelity defects found, so a gap-closure plan can be scoped if needed.
</output>
