---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/renderer/brand/tokens.json
  - src/renderer/src/brand/fonts/jost-latin.woff2
  - src/renderer/src/brand/fonts/jost-latin-ext.woff2
  - src/renderer/src/brand/fonts/dm-sans-latin.woff2
  - src/renderer/src/brand/fonts/dm-sans-latin-ext.woff2
  - src/renderer/src/globals.css
  - components.json
  - src/renderer/src/lib/utils.ts
  - src/renderer/src/components/ui/button.tsx
  - src/renderer/src/components/ui/badge.tsx
  - src/renderer/src/components/ui/separator.tsx
  - src/renderer/src/components/ui/tooltip.tsx
autonomous: true
requirements: [BRAND-01]
must_haves:
  truths:
    - "The renderer is themed with the Magnet Group tokens: light and dark palettes both wired"
    - "Jost and DM Sans are served locally from bundled woff2 files, never from a CDN"
    - "Foreground normalizes to #343434 and muted-foreground to #6e6e73 per the canonical tokens"
    - "shadcn is initialized against the renderer globals.css with base-nova and lucide"
  artifacts:
    - path: "src/renderer/src/globals.css"
      provides: "Tailwind v4 @theme plus :root light palette plus .dark palette plus @font-face"
      contains: "@theme"
    - path: "src/renderer/brand/tokens.json"
      provides: "Vendored copy of the canonical Magnet Group tokens (build-time source of truth)"
      contains: "brand"
    - path: "components.json"
      provides: "shadcn config for the Vite renderer (rsc false, css points at renderer globals.css)"
      contains: "base-nova"
    - path: "src/renderer/src/lib/utils.ts"
      provides: "cn() class helper"
      contains: "twMerge"
  key_links:
    - from: "src/renderer/src/globals.css"
      to: "src/renderer/src/brand/fonts"
      via: "@font-face src url"
      pattern: "@font-face"
    - from: "components.json"
      to: "src/renderer/src/globals.css"
      via: "tailwind.css path"
      pattern: "globals.css"
---

<objective>
Wire the Magnet Group brand system into the renderer: vendor the canonical `tokens.json`, bundle the Jost and DM Sans woff2 fonts locally, author `globals.css` (Tailwind v4 `@theme` plus light `:root` and net-new `.dark` palettes plus `@font-face`), initialize shadcn against the renderer, and add the conservative shadcn component set. This is the BRAND-01 theme seam every UI phase consumes.

Purpose: The brand token file is canonical; this plan renders it faithfully into the app so no later phase invents colors or fonts. Both palettes are wired now so OS light/dark just works once the shell mirrors it (01-06).
Output: A fully themed renderer stylesheet, bundled fonts, and branded shadcn primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
Canonical brand source files to read and adapt (all confirmed present on 2026-07-22):
- ../themagnetgroup/brand-guide/agents/tokens.json (canonical values to vendor, D-02)
- ../themagnetgroup_website/src/app/globals.css (Tailwind v4 @theme inline plus :root structure to adapt)
- ../themagnetgroup_website/components.json (shadcn config shape to adapt)
- ../themagnetgroup_website/src/lib/utils.ts (cn helper, copy verbatim)
- ../themagnetgroup_website/public/fonts/{jost-latin,jost-latin-ext,dm-sans-latin,dm-sans-latin-ext}.woff2 (font binaries to copy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor tokens and fonts, copy cn helper, init shadcn, add component set</name>
  <files>src/renderer/brand/tokens.json, src/renderer/src/brand/fonts/jost-latin.woff2, src/renderer/src/brand/fonts/jost-latin-ext.woff2, src/renderer/src/brand/fonts/dm-sans-latin.woff2, src/renderer/src/brand/fonts/dm-sans-latin-ext.woff2, src/renderer/src/lib/utils.ts, components.json, src/renderer/src/components/ui/button.tsx, src/renderer/src/components/ui/badge.tsx, src/renderer/src/components/ui/separator.tsx, src/renderer/src/components/ui/tooltip.tsx</files>
  <read_first>
    - 01-PATTERNS.md components.json section (lines 178-208): the reference config plus the exact Phase 1 deltas (rsc false, css path, base-nova, neutral, lucide, registries {})
    - 01-PATTERNS.md lib/utils.ts section (lines 212-223): copy verbatim
    - 01-PATTERNS.md shadcn copy-ins section (lines 227-229) and Caveat A (line 297): regenerate via CLI, expect base-ui not classic Radix, do not hardcode primitive deps
    - 01-PATTERNS.md Caveat B (line 299): copy the four woff2 binaries but the loader is @font-face, not next/font/local
    - 01-CONTEXT.md D-02 and D-04: vendor tokens; fonts bundled locally, never CDN
    - ../themagnetgroup/brand-guide/agents/tokens.json (the file to copy into the repo)
    - ../themagnetgroup_website/components.json and ../themagnetgroup_website/src/lib/utils.ts (analogs)
  </read_first>
  <action>
    Copy ../themagnetgroup/brand-guide/agents/tokens.json verbatim into src/renderer/brand/tokens.json as the vendored build-time source of truth per D-02; do not reference the sibling repo at build time. Copy the four woff2 binaries from ../themagnetgroup_website/public/fonts into src/renderer/src/brand/fonts per D-04. Copy ../themagnetgroup_website/src/lib/utils.ts verbatim into src/renderer/src/lib/utils.ts (the cn helper using clsx plus tailwind-merge). Run `npx shadcn@latest init` then `npx shadcn@latest add button badge separator tooltip`, then edit components.json to the Phase 1 deltas: rsc false, tailwind.css pointing at src/renderer/src/globals.css, keep style base-nova, baseColor neutral, cssVariables true, iconLibrary lucide, registries empty, aliases @/components, @/lib/utils, @/components/ui, @/lib, @/hooks. Accept whatever primitive dependency the base-nova CLI resolves (base-ui or radix) and pin what it installs; do not hand-add @radix-ui/react-slot or @base-ui/react per Caveat A. Ensure the @/* aliases resolve through both the Vite resolve.alias from 01-01 and the renderer tsconfig paths. No em or en dashes in any authored file.
  </action>
  <acceptance_criteria>
    - src/renderer/brand/tokens.json is byte-identical to the sibling canonical tokens.json (same brand.primary #6c00ff, surface.fg #343434)
    - The four woff2 font files exist under src/renderer/src/brand/fonts
    - src/renderer/src/lib/utils.ts exports cn using twMerge and clsx
    - components.json has rsc false, style base-nova, iconLibrary lucide, and tailwind.css pointing at the renderer globals.css
    - src/renderer/src/components/ui contains button.tsx, badge.tsx, separator.tsx, tooltip.tsx generated by the CLI
    - tokens.json and the fonts are NOT gitignored (they are committed brand assets, not secrets)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('src/renderer/brand/tokens.json');const b=fs.readFileSync('../themagnetgroup/brand-guide/agents/tokens.json');if(Buffer.compare(a,b)===0){console.log('TOKENS_VENDORED_OK')}else{process.exit(1)}"</automated>
  </verify>
  <done>The canonical tokens and fonts are vendored into the repo, the cn helper is in place, and shadcn is initialized with the four branded primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Author globals.css - Tailwind v4 theme, light and dark palettes, local fonts</name>
  <files>src/renderer/src/globals.css</files>
  <read_first>
    - 01-PATTERNS.md globals.css section (lines 106-174): the six Phase 1 deltas, the exact light :root values, the net-new .dark block, the @font-face wiring, the @layer base adaptation, and the structural-radius note
    - 01-RESEARCH.md Pattern 5 (lines 418-429): the Tailwind v4 wiring key adaptations for the Electron renderer
    - 01-UI-SPEC.md Color (lines 96-138), Typography (lines 81-91), and Radius Usage (lines 142-151): every declared value, verbatim
    - 01-CONTEXT.md D-03 (both palettes), D-04 (local fonts), D-05 (#343434 foreground), D-06 (carry semantic and chart colors now)
    - ../themagnetgroup_website/src/app/globals.css (structure to adapt, but author values from the vendored tokens, not the reference :root, per Caveat C)
    - src/renderer/brand/tokens.json (vendored, from Task 1) for canonical values
  </read_first>
  <action>
    Author src/renderer/src/globals.css from the reference structure but with values from the vendored tokens.json. Keep the top-of-file `@import "tailwindcss";` and `@import "tw-animate-css";` and the `@custom-variant dark (&:is(.dark *));`, and keep whatever base layer `npx shadcn init` wrote (do not hand-copy the Next-specific shadcn import per PATTERNS lines 110-117). Reuse the `@theme inline` `--color-*` and `--radius-*` mapping block, and add `--font-heading`, `--font-sans`, `--font-mono` mappings. Carry the semantic colors (success #28a745, warning #f59e0b, info #6c00ff, destructive #ff3b30) and the five chart colors from tokens.json into `@theme` now per D-06. Author the light `:root` block with the exact values in PATTERNS lines 122-143: background #ffffff, foreground #343434 (D-05, NOT #1d1d1f), card #ffffff, primary #6c00ff, secondary #f5f5f7, muted-foreground #6e6e73 (D-01, NOT #48484a), accent #c77dff, destructive #ff3b30, border #e5e5ea, input #e5e5ea, ring #6c00ff, radius 0.625rem. Author the net-new `.dark {}` block with PATTERNS lines 147-163 values: background #1a1a1a, foreground #f0f0f0, card #242424, popover #2a2a2a, secondary #2a2a2a, muted-foreground #a0a0a0, border #3a3a3a, input #3a3a3a, ring #8f33ff. Author `@font-face` blocks pointing at the vendored src/renderer/src/brand/fonts woff2 files for Jost and DM Sans; set `--font-heading` to the Jost family string and `--font-sans` to the DM Sans family string from tokens.json font.family (never a Google Fonts import or link, D-04). Keep the `@layer base` `* { border-border outline-ring/50 }` and `body { bg-background text-foreground }` rules; drop the website-only scroll-margin and section rules. Keep the `prefers-reduced-motion` block verbatim (UI-SPEC Motion). Do not apply a blanket radius: structural surfaces use radius 0, cards/inputs use the radiusUsage ladder. No dashes in the file.
  </action>
  <acceptance_criteria>
    - globals.css defines both a `:root` block and a `.dark` block (both palettes wired, D-03)
    - The light `--foreground` is `#343434` and `--muted-foreground` is `#6e6e73` (canonical, not the reference site values)
    - The dark `--ring` is `#8f33ff` and dark `--background` is `#1a1a1a`
    - globals.css contains `@font-face` rules whose `src` points at ./brand/fonts/*.woff2 and contains no `fonts.googleapis.com` or Google Fonts `@import`
    - globals.css includes the semantic (success/warning/info/destructive) and five chart colors in `@theme`
    - `npm run build` compiles the renderer CSS with no Tailwind errors
    - No em or en dash characters appear in globals.css
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node -e "const s=require('fs').readFileSync('src/renderer/src/globals.css','utf8'); const ok = /:root/.test(s) && /\.dark/.test(s) && /#343434/.test(s) && /#8f33ff/.test(s) && /@font-face/.test(s) && !/googleapis/.test(s) && !/[–—]/.test(s); process.exit(ok?0:1)" && echo GLOBALS_OK</automated>
  </verify>
  <done>The renderer stylesheet renders the canonical brand tokens in both light and dark, serves fonts locally, and carries forward semantic/chart colors for later phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build input to renderer bundle | Vendored token file and font binaries are copied into the repo; no runtime fetch, no external CDN, no cross-repo build dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | remote font/asset fetch | mitigate | Fonts are bundled woff2 served locally via @font-face; no Google Fonts import or link (D-04); offline desktop tool loads local content only (Task 2) |
| T-01-10 | Information Disclosure | vendored brand assets | accept | tokens.json and woff2 files are non-secret brand assets, committed intentionally; no credential material is present in this layer |
</threat_model>

<verification>
- `npm run build` compiles the renderer with the theme applied and no Tailwind errors
- globals.css contains both `:root` and `.dark` palettes with the canonical values
- No Google Fonts reference exists in globals.css
- The vendored tokens.json matches the canonical source byte-for-byte
</verification>

<success_criteria>
The renderer is themed faithfully to the Magnet Group tokens in both light and dark, fonts are local, and shadcn primitives are branded. (Advances BRAND-01; the visual application to the shell lands in 01-06.)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Record the CSS variable names available to components (semantic classes like bg-secondary, text-foreground, text-primary, ring-ring) and the primitive dependency the base-nova CLI resolved (base-ui vs radix), so 01-06 wires components without surprise.
</output>
