---
phase: 03-ai-client-and-parse-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/main/ai/client.ts
  - src/main/ai/models.ts
  - src/main/ai/vision-families.ts
  - src/main/ipc/ai.ts
  - src/main/ipc/register.ts
  - src/renderer/src/screens/SettingsScreen.tsx
  - test/ai-models.test.ts
  - test/no-secret-leak.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04]
user_setup:
  - service: openai-compatible-endpoint
    why: "Live model-list + connection test needs a user-supplied OpenAI-compatible API key and base URL (per CLAUDE.md). Deterministic layers are covered by injected fakes; the live path is the end-of-phase manual gate."
    env_vars: []
    dashboard_config:
      - task: "Obtain an OpenAI-compatible API key and base URL (OpenAI or OpenRouter) and enter them in Settings"
        location: "The provider's dashboard (e.g. platform.openai.com or openrouter.ai/keys)"
must_haves:
  truths:
    - "User can enter an API key + base URL in Settings; both are stored via the OS keychain (secrets channel), never SQLite/plaintext"
    - "A Connect/Test action calls /models exactly once and both validates the credentials and populates the model picker"
    - "The AI-connection status renders OK/error mirroring the existing Secret store HealthIndicator"
    - "Vision-capable models are badged; selecting an unbadged model requires an explicit use-anyway confirm"
    - "The selected model id persists in app_settings and can be changed at any time"
  artifacts:
    - path: "src/main/ai/models.ts"
      provides: "listModels() + classifyVision() metadata-first then curated fallback"
      exports: ["listModels", "classifyVision"]
    - path: "src/main/ai/client.ts"
      provides: "buildClient(): reads key/baseURL from secret-store, validates https base URL, new OpenAI(maxRetries:3, timeout:120000)"
      exports: ["buildClient"]
    - path: "src/main/ipc/ai.ts"
      provides: "ai:test-connection / ai:list-models / ai:set-model handlers"
      exports: ["registerAiIpc"]
    - path: "src/renderer/src/screens/SettingsScreen.tsx"
      provides: "AI-config section (presets+custom URL, key field, Connect/Test, status, model picker with vision badges + confirm gate)"
      contains: "testConnection"
  key_links:
    - from: "src/renderer/src/screens/SettingsScreen.tsx"
      to: "ai:test-connection"
      via: "window.api.ai.testConnection"
      pattern: "window\\.api\\.ai\\.testConnection"
    - from: "src/main/ipc/ai.ts"
      to: "src/main/secrets/secret-store.ts"
      via: "secretStore.get('ai-api-key') / get('ai-base-url')"
      pattern: "secretStore\\.get"
---

<objective>
Deliver the complete AI-configuration vertical slice: the user enters an OpenAI-compatible key + base URL in Settings, presses Connect/Test which reads the credentials main-side and calls /models once (validating the connection and populating a classified model picker), sees a vision badge on capable models with a use-anyway confirm on unbadged ones, and persists their selected model. Satisfies AI-01..04.

Purpose: This is the first fully demoable Phase 3 capability — a real end-to-end config flow through the trust boundary. The key never crosses IPC in either direction.
Output: The src/main/ai/ service layer, the ai IPC handlers, and the Settings AI-config UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** non-technical bookkeeper, **I want to** configure a vision AI model once and have the app read each scanned bill into validated vendor/date/amount fields with confidence flags, **so that** I can review accurate entries instead of hand-typing every bill.

This plan delivers the "configure a vision AI model once" half of that story end-to-end.

<context>
@.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md
@src/main/ipc/settings.ts
@src/main/ipc/secrets.ts
@src/main/ipc/ingestion.ts
@src/main/ipc/register.ts
@src/main/secrets/secret-store.ts
@src/renderer/src/screens/SettingsScreen.tsx
@src/renderer/src/components/HealthIndicator.tsx
@test/no-secret-leak.test.ts
@test/helpers/fake-openai-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): spec vision classification, model list/persist, and the AI-key no-leak canary</name>
  <read_first>
    - test/helpers/fake-openai-client.ts (the shared double this test drives — OpenAI-minimal vs OpenRouter-rich shapes, `.calls` recorder)
    - test/no-secret-leak.test.ts (whole file — the canary/mock-electron pattern; note it already writes key 'ai-api-key' at line 50-60, extend to prove baseURL too and the classifier path)
    - test/ai-models.test.ts location conventions from test/ingestion-scan.test.ts (temp DB + migrate pattern for the set-model persistence assertion)
    - 03-CONTEXT.md D-01/D-02 (flag+confirm, metadata-first then curated), D-04 (test calls /models once), D-05 (key/baseURL in keychain, model id in app_settings), D-25 (classifyVision order)
    - 03-RESEARCH.md Directive 6 (classifyVision pseudocode lines 430-437; curated families lines 434-436) and 03-PATTERNS.md sections "src/main/ai/models.ts", "src/main/ai/vision-families.ts"
  </read_first>
  <files>test/ai-models.test.ts, test/no-secret-leak.test.ts</files>
  <action>
    Create test/ai-models.test.ts driving the shared fake client:
    (a) classifyVision: an OpenRouter-rich model whose `architecture.input_modalities` includes `'image'` -> `'vision'`; an OpenAI-minimal model whose id matches a curated family (e.g. `gpt-4o`, `claude-3-5-sonnet`, `gemini-1.5-pro`, `qwen2.5-vl`) -> `'vision-family'`; an unknown id (`text-davinci-003`, `some-random-llm`) -> `'unknown'`.
    (b) listModels: feeds a mixed list through the fake client's `models.list()` and asserts each entry is returned with the right `vision` classification, and that `models.list()` is called exactly once.
    (c) set-model persistence: writing the selected model id via the settings prepared-statement path (against a migrated temp DB) round-trips through `settings:get` (non-secret, AI-04).
    Extend test/no-secret-leak.test.ts: add a case that stores BOTH `secretStore.set('ai-api-key', CANARY)` and `secretStore.set('ai-base-url', 'https://canary.example/v1')` and asserts neither the key CANARY nor a distinct base-url canary appears in secrets.enc plaintext, app.db, or captured logs (extends the existing three-surface scan to the AI credentials, D-05).
  </action>
  <behavior>
    - classifyVision(openrouterImageModel) === 'vision'
    - classifyVision(minimalGpt4o) === 'vision-family'
    - classifyVision(unknownModel) === 'unknown'
    - listModels() invokes client.models.list() exactly once and classifies every entry
    - selected model id written then read via settings round-trips
    - AI key + base-url canaries never appear in secrets.enc/app.db/logs
  </behavior>
  <verify>
    <automated>npx vitest run test/ai-models.test.ts test/no-secret-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/ai-models.test.ts FAILS to import src/main/ai/models.ts and src/main/ai/vision-families.ts before Task 2 (RED).
    - The extended no-secret-leak test asserts absence of both AI credential canaries across all three surfaces.
  </acceptance_criteria>
  <done>The classification, list/persist, and AI-key no-leak specs exist and are RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): AI service layer + ai IPC handlers</name>
  <read_first>
    - src/main/ipc/settings.ts (lines 19-41 — the prepared-statement settings:set path ai:set-model reuses)
    - src/main/ipc/secrets.ts (lines 20-43 — the "read from secret-store, no secret crosses the boundary" discipline; graceful-null on unavailable)
    - src/main/ipc/ingestion.ts (lines 20-44 — the async handler shape assertTrustedSender -> parse -> work)
    - src/main/ipc/register.ts (the one-line-per-group registration)
    - src/main/secrets/secret-store.ts (get() returning string|null, lines 56-61)
    - 03-PATTERNS.md Shared Pattern A + sections "src/main/ai/client.ts", "src/main/ai/models.ts", "src/main/ai/vision-families.ts", "src/main/ipc/ai.ts"
    - 03-RESEARCH.md Directive 6 (D-25 client config + fallback; classifyVision; ModelInfoSchema lenient) and Security V14 (validate well-formed https base URL, do not follow redirects with the key)
  </read_first>
  <files>src/main/ai/vision-families.ts, src/main/ai/models.ts, src/main/ai/client.ts, src/main/ipc/ai.ts, src/main/ipc/register.ts</files>
  <action>
    ai/vision-families.ts: export a pure `isKnownVisionFamily(id: string): boolean` matching the curated regex families from RESEARCH lines 434-436 (gpt-4o, gpt-4.1, o1/o3/o4 vision, claude-3/3.5/opus/sonnet, gemini-1.5/2/flash/pro, llama-3.2-vision, qwen2-vl/qwen2.5-vl, pixtral). Zero side effects (mirror ingestion/hash.ts's tiny-pure-module convention).
    ai/models.ts: export `classifyVision(model): 'vision' | 'vision-family' | 'unknown'` implementing D-25 order — (1) `model.architecture?.input_modalities` includes `'image'` -> 'vision'; (2) `isKnownVisionFamily(model.id)` -> 'vision-family'; (3) 'unknown'. Export `listModels(deps)` that calls `deps.client.models.list()`, Zod-parses each entry with a lenient `ModelInfoSchema` (all rich fields optional so OpenRouter extras survive and OpenAI-minimal degrades), maps through classifyVision, returns `ModelInfo[]`. Injectable `client` per Shared Pattern B.
    ai/client.ts: export `buildClient(deps)` that reads `secretStore.get('ai-api-key')` and `secretStore.get('ai-base-url')` (secretStore injectable), throws a plain recoverable error if either is missing, validates the base URL parses as a well-formed `https:` URL via `new URL()` (reject non-https — SSRF/key-exfil guard T-03-05), and returns `new OpenAI({ apiKey, baseURL, maxRetries: 3, timeout: 120000 })` (D-25). Never log the key.
    ipc/ai.ts: export `registerAiIpc()` with three handlers, each `assertTrustedSender(event)` FIRST then Zod-parse (Shared Pattern A): `ai:test-connection` (parse AiTestConnectionSchema empty payload; buildClient; listModels once — D-04 validate+populate in one call; return `{ ok: true, models }`; on any thrown error return `{ ok: false }` — plain recoverable, never a raw stack, mirror secrets.ts graceful-null); `ai:list-models` (same list+classify path); `ai:set-model` (parse AiSetModelSchema; write the model id to app_settings via the settings prepared-statement UPSERT under key `ai-model`). The key is read main-side only and NEVER returned across IPC (D-05/T-03-01).
    register.ts: add `import { registerAiIpc } from './ai'` and call `registerAiIpc()` inside `registerIpc()`.
  </action>
  <behavior>
    - buildClient rejects a non-https base URL and a missing key with a recoverable error
    - ai:test-connection returns { ok:false } (no stack) when the client throws; { ok:true, models } on success, calling models.list() once
    - ai:set-model persists under app_settings key 'ai-model'; the key/baseURL never appear in any return value
  </behavior>
  <verify>
    <automated>npx vitest run test/ai-models.test.ts test/no-secret-leak.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test/ai-models.test.ts and test/no-secret-leak.test.ts PASS (GREEN).
    - `grep -n "console\.\|logger" src/main/ai/client.ts src/main/ipc/ai.ts` shows no logging of key/baseURL values.
    - buildClient uses `new URL()` and rejects a `http://` base URL (source assertion in models/client test or a dedicated case).
    - register.ts calls registerAiIpc().
  </acceptance_criteria>
  <done>The AI service layer + handlers are wired and green; the key stays main-side.</done>
</task>

<task type="auto">
  <name>Task 3: Settings AI-config section (presets+custom URL, key field, Connect/Test, status, model picker + confirm gate)</name>
  <read_first>
    - src/renderer/src/screens/SettingsScreen.tsx (whole file — extend the placeholder at lines 60-62; reuse the recoverable-error block lines 73-80 and the async-effect + cancelled guard lines 24-38)
    - src/renderer/src/components/HealthIndicator.tsx (lines 47-81 — the OK/error status structure D-04 mirrors: text-success/text-destructive, rounded-xl border bg-card card)
    - src/renderer/src/components/ui/badge.tsx (variants: default|secondary|destructive|outline|ghost|link — for the vision badge)
    - src/renderer/src/components/ui/button.tsx
    - 03-CONTEXT.md D-01 (flag+confirm), D-03 (presets+custom base URL), D-04 (Connect/Test -> status), D-18a (reuse branded components, no new design system)
    - 03-PATTERNS.md Shared Pattern E + section "src/renderer/src/screens/SettingsScreen.tsx"
  </read_first>
  <files>src/renderer/src/screens/SettingsScreen.tsx</files>
  <action>
    Replace the "Connection and model settings will appear here" placeholder (lines 60-62) with an AI-config section using ONLY existing branded components and semantic theme classes (no hardcoded hex):
    - Base-URL control (D-03): a select of presets — OpenAI (`https://api.openai.com/v1`), OpenRouter (`https://openrouter.ai/api/v1`), and Custom (reveals a free-text input). The chosen URL is written to the keychain via `window.api.secrets.set('ai-base-url', url)`.
    - API-key input (masked) written via `window.api.secrets.set('ai-api-key', key)`. The key value is held only in transient local state for the input; never rendered back after save.
    - A "Connect and test" Button calling `window.api.ai.testConnection()`. On `{ ok:true, models }` render an "AI connection: OK" status mirroring HealthIndicator (text-success, ShieldCheck-style card) and populate the model picker; on `{ ok:false }` or a thrown error render "AI connection: error" (text-destructive) plus the recoverable-error block (same shape as lines 73-80). Exactly one /models call per press (D-04).
    - Model picker: render the returned `ModelInfo[]`; each model with `vision` of `'vision'`/`'vision-family'` gets a `Badge` (variant default/secondary) labelled "Vision". Selecting an `unknown` model opens a confirm gate ("This model is not confirmed vision-capable. Use anyway?") that must be accepted before selection sticks (D-01). On confirmed selection call `window.api.ai.setModel(id)` (AI-04, persists to app_settings) and reflect the current selection on next load via `window.api.settings.get('ai-model')`.
    All async loads use the `cancelled` guard pattern from lines 24-38.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <human-check>At the end-of-phase human gate: launch the app, open Settings; confirm the AI-config section renders (preset dropdown, key field, Connect and test button, model picker). With a real key+base URL: Connect and test shows "AI connection: OK" and populates the picker; vision models show a badge; selecting an unbadged model prompts the use-anyway confirm; the selected model persists across a Settings reopen. (VALIDATION.md Manual-Only row 3.)</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - `grep -n "#\|rgb(\|hsl(" src/renderer/src/screens/SettingsScreen.tsx` shows no hardcoded color literals (semantic classes only).
    - Source contains `window.api.ai.testConnection`, `window.api.ai.setModel`, `window.api.secrets.set('ai-api-key'`, and `window.api.secrets.set('ai-base-url'`.
    - The API key is never passed to a render/log path (no `{apiKey}` in JSX after save).
  </acceptance_criteria>
  <done>The Settings AI-config flow is complete and typechecks; visual/live behavior is confirmed at the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main (ai channels) | Key/baseURL are set via the secrets channel; test/list/set never receive or return the key |
| main -> user-configured endpoint | An outbound HTTPS call carries the API key to a user-chosen base URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ai:test-connection / ai:list-models / client.ts | mitigate | Key + baseURL read from secret-store main-side only, never returned across IPC, never logged; no-secret-leak test extended to both AI credential canaries (Task 1/2); grep gate asserts no logging |
| T-03-05 | Information Disclosure (SSRF / key exfil) | src/main/ai/client.ts | mitigate | buildClient validates a well-formed `https:` base URL via `new URL()` and rejects non-https before instantiating the client; do not send the key to a non-https / malformed target (RESEARCH Security V14) |
| T-03-01b | Information Disclosure | SettingsScreen key input | mitigate | Key held only in transient input state, written straight to the keychain via secrets.set, never re-rendered or echoed after save |
</threat_model>

<verification>
- `npx vitest run test/ai-models.test.ts test/no-secret-leak.test.ts` green.
- `npm run typecheck` green.
- End-of-phase human gate confirms the Settings AI-config flow and live /models call.
</verification>

<success_criteria>
- User can store key+baseURL in the keychain, Connect/Test calls /models once and shows OK/error, and the classified model picker appears (AI-01, AI-02).
- Vision models are badged and unbadged models require confirm (AI-03).
- Selected model persists and is changeable (AI-04).
- The API key never crosses IPC or reaches logs.
</success_criteria>

<output>
Create `.planning/phases/03-ai-client-and-parse-pipeline/03-02-SUMMARY.md` when done.
</output>
