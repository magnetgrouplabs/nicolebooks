---
phase: 03-ai-client-and-parse-pipeline
plan: 06
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/main/db/migrations/0003_parsed_results.ts
  - src/main/db/migrate.ts
  - src/main/parse/cache.ts
  - test/migrate.test.ts
  - test/parse-cache.test.ts
autonomous: true
requirements: [PARSE-05]
must_haves:
  truths:
    - "migration0003 creates the parsed_results STRICT table keyed on file_hash and the runner reaches user_version 3"
    - "The cache reads and writes by file_hash using prepared statements with bound (never interpolated) values"
    - "The cache is keyed on file_hash ALONE — storing a different model value does not invalidate the row; only a schema_version bump or explicit re-parse does"
    - "field_confidence and validation_flags persist as JSON TEXT blobs; money persists as INTEGER cents; no boolean columns"
  artifacts:
    - path: "src/main/db/migrations/0003_parsed_results.ts"
      provides: "the parsed_results STRICT table (D-24 DDL)"
      contains: "parsed_results"
    - path: "src/main/parse/cache.ts"
      provides: "getCached(hash) + putCached(row) prepared-statement CRUD (cache-first, cache-last)"
      exports: ["getCached", "putCached"]
  key_links:
    - from: "src/main/parse/cache.ts"
      to: "parsed_results"
      via: "prepared SELECT/INSERT WHERE file_hash = ?"
      pattern: "file_hash"
---

<objective>
Add the parsed-results persistence layer: the forward-only migration0003 creating the parsed_results STRICT table (keyed on the Phase 2 SHA-256 hash) and the cache module that reads/writes it with bound prepared statements. This is the PARSE-05 substrate — a cache hit means the paid model is never re-called for the same bytes.

Purpose: Cache-first, cache-last correctness. Keyed on the hash alone so a model switch never silently re-charges (D-14/Pitfall 7); schema_version lets a deliberate prompt/schema bump force re-parse.
Output: src/main/db/migrations/0003_parsed_results.ts, migrate.ts (append), src/main/parse/cache.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** non-technical bookkeeper, **I want to** configure a vision AI model once and have the app read each scanned bill into validated vendor/date/amount fields with confidence flags, **so that** I can review accurate entries instead of hand-typing every bill.

This plan delivers the "persist parsed results so a reload/crash never re-calls the paid model" guarantee (success criterion 5, storage half).

<context>
@.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md
@src/main/db/migrate.ts
@src/main/db/migrations/0002_dedupe.ts
@src/main/ipc/settings.ts
@src/main/ingestion/ledger.ts
@test/migrate.test.ts
@src/shared/ipc-contract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): spec the migration schema and the hash-keyed cache CRUD</name>
  <read_first>
    - test/migrate.test.ts (whole file — extend the user_version, table-set, and column-name assertions; note the current expectations at lines 46-91 stop at version 2 / ['app_settings','posted_file_hashes'])
    - src/main/db/migrations/0002_dedupe.ts (the STRICT-table + `hash TEXT PRIMARY KEY` idiom to mirror)
    - src/main/ingestion/ledger.ts (checkPostedHash — the prepared bound SELECT WHERE hash = ? convention)
    - 03-CONTEXT.md D-24 (exact columns) and D-14 (key on file_hash alone; model switch does not invalidate)
    - 03-RESEARCH.md Directive 5 DDL (lines 393-414) and Pitfall 7 (cache-key-on-hash-alone) and Pitfall 8 (STRICT: no boolean)
    - 03-VALIDATION.md rows PARSE-05/D-14 and PARSE-05/D-24
  </read_first>
  <files>test/migrate.test.ts, test/parse-cache.test.ts</files>
  <action>
    Extend test/migrate.test.ts: after migrate() user_version is 3; the table set is exactly `['app_settings','posted_file_hashes','parsed_results']`; parsed_results columns equal the D-24 list (file_hash, original_filename, route, page_count, model, base_url_host, vendor, invoice_number, invoice_date, due_date, subtotal_cents, tax_cents, total_cents, currency, suggested_category, field_confidence, validation_flags, raw_response, parsed_at, schema_version); a second migrate() stays idempotent at 3.
    Create test/parse-cache.test.ts (against a migrated temp DB, mirroring migrate.test.ts lifecycle): putCached then getCached round-trips a full row including field_confidence/validation_flags as JSON and money as integer cents; getCached on an absent hash returns null; a putCached that OVERWRITES the same file_hash with a DIFFERENT model value keeps ONE row (upsert by hash) and does NOT error — proving hash-alone keying (D-14); base_url_host stores only a host, never a key. (The cache-hit-no-recall pipeline behavior is added in 03-07; this file proves the storage layer.)
  </action>
  <verify>
    <automated>npx vitest run test/migrate.test.ts test/parse-cache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/migrate.test.ts FAILS on the version-3 / parsed_results assertions before Task 2 (RED).
    - test/parse-cache.test.ts FAILS to import src/main/parse/cache.ts before Task 2 (RED).
    - The cache spec explicitly proves single-row-by-hash across a model change.
  </acceptance_criteria>
  <done>The migration + cache specs exist and are RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): migration0003 + cache.ts prepared-statement CRUD</name>
  <read_first>
    - src/main/db/migrations/0002_dedupe.ts (copy the `export const migrationNNNN = { version, up(db){ db.exec(...) } }` STRICT shape)
    - src/main/db/migrate.ts (append migration0003 to the array; never renumber)
    - src/main/ipc/settings.ts (lines 22-26 — the prepared get/upsert statement idiom cache.ts copies)
    - src/main/ingestion/ledger.ts (bound-parameter SELECT WHERE hash = ?)
    - src/shared/ipc-contract.ts (ParsedFields / FieldConfidence types the cache row maps to)
    - 03-RESEARCH.md Directive 5 DDL (lines 393-414 — use verbatim) and 03-PATTERNS.md section "src/main/db/migrations/0003_parsed_results.ts" (lines 384-413) + "src/main/parse/cache.ts"
    - 03-CONTEXT.md D-24
  </read_first>
  <files>src/main/db/migrations/0003_parsed_results.ts, src/main/db/migrate.ts, src/main/parse/cache.ts</files>
  <action>
    0003_parsed_results.ts: export `migration0003 = { version: 3, up(db){ db.exec(`CREATE TABLE IF NOT EXISTS parsed_results (...) STRICT;`) } }` using the D-24 DDL from RESEARCH lines 393-414: `file_hash TEXT PRIMARY KEY`, original_filename TEXT NOT NULL, route TEXT NOT NULL, page_count INTEGER NOT NULL, model TEXT NOT NULL, base_url_host TEXT, vendor/invoice_number/invoice_date/due_date/currency/suggested_category TEXT, subtotal_cents/tax_cents INTEGER (nullable), total_cents INTEGER NOT NULL, field_confidence TEXT NOT NULL, validation_flags TEXT, raw_response TEXT, parsed_at TEXT NOT NULL, schema_version INTEGER NOT NULL. No boolean columns (Pitfall 8). Comment that no secret material lands here — base_url_host is host-only (D-05).
    migrate.ts: `import { migration0003 }` and append it to the `migrations` array (now `[migration0001, migration0002, migration0003]`); do not renumber.
    cache.ts: export `getCached(db, fileHash): CachedResult | null` (prepared `SELECT * FROM parsed_results WHERE file_hash = ?`, bound) and `putCached(db, row)` (prepared INSERT ... ON CONFLICT(file_hash) DO UPDATE, named parameters, never interpolated — settings.ts idiom). Serialize field_confidence/validation_flags to JSON TEXT on write and parse on read; keep money as INTEGER cents; store a `SCHEMA_VERSION` module constant (start at 1) into schema_version. `db` is injectable (tests pass a temp DB). Extract `base_url_host` from the base URL host only if a caller supplies it — never persist the key.
  </action>
  <behavior>
    - migrate() reaches user_version 3 and creates parsed_results with the exact D-24 columns
    - getCached/putCached round-trip a full row keyed on file_hash; absent hash -> null
    - an upsert with a new model value updates the single existing row (hash-alone keying)
  </behavior>
  <verify>
    <automated>npx vitest run test/migrate.test.ts test/parse-cache.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both specs PASS (GREEN); user_version is 3 and parsed_results has all 20 D-24 columns.
    - `grep -n "file_hash = ?\|@file_hash\|ON CONFLICT(file_hash)" src/main/parse/cache.ts` confirms bound/prepared access (no interpolation) — SQLi guard.
    - No column is declared BOOLEAN and no boolean is bound; money columns are INTEGER.
  </acceptance_criteria>
  <done>The parsed_results table and the hash-keyed cache module are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed data -> local DB | Bill-derived fields (business data) and provenance are written to the local SQLite cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering (SQL injection) | cache.ts | mitigate | Prepared statements only, STRICT table; file_hash and all values are bound, never interpolated (mirrors the ledger/settings pattern) |
| T-03-01 | Information Disclosure | parsed_results row | mitigate | No secret material persists — base_url_host is host-only, never the API key (D-05/D-24); raw_response holds bill business data (not credentials) in the OS userData dir |
</threat_model>

<verification>
- `npx vitest run test/migrate.test.ts test/parse-cache.test.ts` green.
- `npm run typecheck` green.
</verification>

<success_criteria>
- migration0003 creates parsed_results STRICT keyed on file_hash; runner reaches user_version 3 (PARSE-05, D-24).
- Cache CRUD is bound/prepared and keyed on hash alone; a model switch does not invalidate (D-14).
- No secret material and no boolean columns land in the table.
</success_criteria>

<output>
Create `.planning/phases/03-ai-client-and-parse-pipeline/03-06-SUMMARY.md` when done.
</output>
