---
phase: 03-ai-client-and-parse-pipeline
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/main/parse/route.ts
  - src/main/parse/extract-pdf.ts
  - src/main/parse/prep-image.ts
  - test/parse-route.test.ts
  - test/parse-prep-image.test.ts
  - test/fixtures/README.md
  - test/fixtures/sideways-exif.jpg
  - test/fixtures/image-only.pdf
autonomous: true
requirements: [PARSE-01, PARSE-02]
must_haves:
  truths:
    - "A born-digital text PDF routes 'native' (embedded text + rendered image); an invisible-OCR-overlay PDF and a bitmap-heavy PDF route 'image-only'"
    - "A whole PDF is native only when at least 50% of its pages are native"
    - "unpdf extracts embedded text for native PDFs; pdfjs + @napi-rs/canvas renders a page to a JPEG bitmap for image-only PDFs"
    - "A HEIC file is decoded before sharp; a sideways EXIF photo is auto-oriented and downscaled to the long-edge target with a max-pixel guard"
  artifacts:
    - path: "src/main/parse/route.ts"
      provides: "Docling-style layered native-vs-scan gate (D-20) over injectable pdf signals"
      exports: ["routePdf", "routeFile"]
    - path: "src/main/parse/extract-pdf.ts"
      provides: "unpdf.extractText + pdfjs render-to-JPEG via configureUnPDF official build + @napi-rs/canvas (D-19)"
      exports: ["extractPdfText", "renderPdfPageImage"]
    - path: "src/main/parse/prep-image.ts"
      provides: "heic-convert -> sharp orient/downscale/re-encode (D-07)"
      exports: ["prepImage"]
  key_links:
    - from: "src/main/parse/extract-pdf.ts"
      to: "@napi-rs/canvas"
      via: "configureUnPDF canvas provider for renderPageAsImage"
      pattern: "canvas|configureUnPDF|renderPageAsImage"
---

<objective>
Build the "get each document into the model's mouth" layer: the Docling-style layered gate that decides native-vs-scan per D-20, the PDF path (embedded-text extraction via unpdf + page-to-JPEG rendering via pdfjs + @napi-rs/canvas), and the image path (HEIC decode -> sharp EXIF-orient/downscale/re-encode). Satisfies PARSE-01 and PARSE-02.

Purpose: Correct routing is the one belt-and-suspenders failure mode guard — pairing junk OCR text poisons the model, so the gate must catch invisible-overlay and bitmap-heavy PDFs and send them image-only.
Output: src/main/parse/route.ts, extract-pdf.ts, prep-image.ts, and the committed fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** non-technical bookkeeper, **I want to** configure a vision AI model once and have the app read each scanned bill into validated vendor/date/amount fields with confidence flags, **so that** I can review accurate entries instead of hand-typing every bill.

This plan delivers the "for a text PDF extract text, for a photo/scan prepare the image" routing (success criterion 3).

<context>
@.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md
@src/main/ingestion/hash.ts
@src/main/ingestion/scan.ts
@src/main/ingestion/filetype.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): spec the layered gate + image prep, and build the fixtures</name>
  <read_first>
    - src/main/ingestion/scan.ts (lines 73-131 — the classify+gate loop convention; injectable deps + per-file style route.ts mirrors)
    - test/ingestion-materialization.test.ts (how OS/library signals are injected deterministically in a unit test — the model for feeding synthetic pdfjs operator-list signals to route.ts)
    - 03-CONTEXT.md D-20 (the exact thresholds: bitmap coverage >= 0.75; invisible-glyph ratio > 0.90; native needs >= 50 non-whitespace chars AND >= 1 embedded font; whole PDF native iff >= 50% pages native), D-07 (HEIC->sharp), D-19 (@napi-rs/canvas render)
    - 03-RESEARCH.md Directive 1 (signals from unpdf.extractText + pdfjs getOperatorList: OPS.paintImageXObject, TextRenderingMode.INVISIBLE=3), Code Example lines 500-519 (prepImage), Pitfall 1 (renderPageAsImage needs configureUnPDF official build + canvas), Pitfall 3/6 (EXIF orient; downscale ~2000 long edge)
    - 03-VALIDATION.md rows PARSE-01/02/D-20 and D-19, plus the fixtures list
  </read_first>
  <files>test/parse-route.test.ts, test/parse-prep-image.test.ts, test/fixtures/README.md, test/fixtures/sideways-exif.jpg, test/fixtures/image-only.pdf</files>
  <action>
    Build fixtures (needs the 03-01 deps): generate `test/fixtures/sideways-exif.jpg` with sharp (a small solid image tagged EXIF orientation 6 via `withMetadata({ orientation: 6 })`), hand-author a minimal one-page `test/fixtures/image-only.pdf` embedding a single image XObject with negligible text (so the gate + render path have a real image-only PDF), and write `test/fixtures/README.md` documenting how each fixture was produced and what gate outcome it proves.
    test/parse-route.test.ts: drive `routePdf` with SYNTHETIC per-page signals injected through route.ts's deps (do NOT require real overlay PDFs for the gate logic): (1) page with bitmapCoverage 0.85 -> 'image-only'; (2) page with invisibleGlyphRatio 0.95 -> 'image-only'; (3) page with 200 non-whitespace chars + 1 embedded font + low bitmap coverage -> 'native'; (4) page with 10 chars -> 'image-only'; (5) a 4-page doc with 3 native pages -> whole doc 'native'; 2-of-4 native -> still 'native' (>=50%); 1-of-4 -> 'image-only'. Plus one real-fixture case: routing `image-only.pdf` end-to-end returns 'image-only' AND `renderPdfPageImage` on it returns a non-empty JPEG Buffer (D-19).
    test/parse-prep-image.test.ts: inject a fake `convert` (heic-convert) into prepImage; for ext `.heic` assert `convert` is called BEFORE sharp (records order) and for `.jpg` assert `convert` is NOT called; feed `sideways-exif.jpg` and assert the output JPEG is auto-oriented (width/height swapped vs the tagged orientation) and its long edge <= 2000.
  </action>
  <verify>
    <automated>npx vitest run test/parse-route.test.ts test/parse-prep-image.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both specs FAIL to import src/main/parse/route.ts / extract-pdf.ts / prep-image.ts before Task 2 (RED).
    - test/fixtures/sideways-exif.jpg and test/fixtures/image-only.pdf exist and are non-empty; README documents their provenance.
    - The gate cases cover all four D-20 branches and the >=50%-pages rule using injected synthetic signals (no dependence on hard-to-author overlay PDFs).
  </acceptance_criteria>
  <done>The routing + prep specs and the committed fixtures exist and are RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): route.ts (layered gate) + extract-pdf.ts (unpdf + canvas render) + prep-image.ts</name>
  <read_first>
    - src/main/ingestion/scan.ts (injectable deps + per-file isolation the modules mirror)
    - src/main/ingestion/hash.ts (byte-access-in-main convention)
    - 03-RESEARCH.md Directive 1 (operator-list signals), Code Example lines 500-519 (prepImage — copy directly), Pitfall 1 (configureUnPDF official build + @napi-rs/canvas provider), Pitfall 6 (LONG_EDGE ~2000, detail high)
    - 03-PATTERNS.md sections "src/main/parse/route.ts", "src/main/parse/extract-pdf.ts", "src/main/parse/prep-image.ts"
    - 03-CONTEXT.md D-20 thresholds, D-19 canvas, D-07 prep
  </read_first>
  <files>src/main/parse/route.ts, src/main/parse/extract-pdf.ts, src/main/parse/prep-image.ts</files>
  <action>
    route.ts: export `routePdf(signals): 'native' | 'image-only'` and `routeFile(deps)` implementing D-20 per page in order: (1) `bitmapCoverage >= 0.75` -> image-only; (2) `invisibleGlyphRatio > 0.90` -> image-only; (3) `nonWhitespaceChars >= 50 && embeddedFontCount >= 1` -> native; (4) else image-only. Whole PDF is native iff `nativePages / totalPages >= 0.5`. Keep the four thresholds as named module constants (`BITMAP_COVERAGE_THRESHOLD = 0.75`, `INVISIBLE_GLYPH_RATIO = 0.90`, `MIN_NATIVE_CHARS = 50`, `NATIVE_PAGE_MAJORITY = 0.5`) tunable via the committed fixtures. The pdf-signal loader is injectable (deps) so tests feed synthetic signals; the real loader reads signals from pdfjs `page.getOperatorList()` (count OPS.paintImageXObject/paintInlineImageXObject/paintImageMaskXObject area for coverage; count setTextRenderingMode arg 3 vs total glyph-show ops for the invisible ratio; setFont presence for embedded fonts) and unpdf.extractText for char counts. Image files (jpg/png/heic) route directly to 'image-only'.
    extract-pdf.ts: export `extractPdfText(bytes)` using `unpdf.extractText(data, { mergePages: false })` returning `{ totalPages, text: string[] }`; export `renderPdfPageImage(bytes, pageIndex)` that calls `configureUnPDF` with the OFFICIAL PDF.js build and a `@napi-rs/canvas` provider (Pitfall 1) then `renderPageAsImage` -> JPEG Buffer. Byte access main-only; injectable loader per Shared Pattern B.
    prep-image.ts: copy the RESEARCH prepImage example (lines 500-519) — `const LONG_EDGE = 2000`; if ext is `.heic`/`.heif` run heic-convert (injectable `convert`) to JPEG FIRST, then `sharp(input).rotate().resize({ width: LONG_EDGE, height: LONG_EDGE, fit: 'inside', withoutEnlargement: true }).jpeg({ quality: 80 })`. Add a decompression-bomb guard: construct sharp with a bounded `limitInputPixels` and rely on `withoutEnlargement` so a hostile image cannot force an enormous raster (T-03-03).
  </action>
  <behavior>
    - routePdf returns image-only for bitmap-heavy / invisible-overlay / low-text pages and native for text pages with fonts
    - whole-PDF verdict follows the >=50%-native-pages rule
    - renderPdfPageImage(image-only.pdf) returns a non-empty JPEG Buffer
    - prepImage runs heic-convert before sharp for .heic and never for .jpg; output long edge <= 2000; EXIF orientation applied
  </behavior>
  <verify>
    <automated>npx vitest run test/parse-route.test.ts test/parse-prep-image.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both specs PASS (GREEN), including the real-fixture render case (D-19).
    - `grep -n "configureUnPDF\|canvas" src/main/parse/extract-pdf.ts` confirms the official-build + canvas provider is wired (Pitfall 1).
    - route.ts exposes the four threshold constants at the documented D-20 values.
    - prep-image.ts sets `limitInputPixels` and `withoutEnlargement` (bomb guard).
  </acceptance_criteria>
  <done>Routing, PDF extraction/render, and image prep are green against injected signals and the real fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbox file bytes -> parser | Untrusted PDF/image bytes are parsed and rasterized in the main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering/Elevation (malicious PDF) | route.ts / extract-pdf.ts | mitigate | Untrusted PDFs are parsed by maintained libs (unpdf/pdfjs) in main only, post-materialization (Phase 2 gate); rendering is bounded per page; a parse throw is surfaced as flag-and-keep by the pipeline (03-07), never a crash |
| T-03-03 | Denial of Service (image decompression bomb) | prep-image.ts | mitigate | sharp `limitInputPixels` guard + `resize(..., withoutEnlargement:true)` bounds the raster; page-count cap enforced downstream (03-07) |
</threat_model>

<verification>
- `npx vitest run test/parse-route.test.ts test/parse-prep-image.test.ts` green.
- `npm run typecheck` green.
- Real-fixture render (image-only.pdf -> JPEG) proves the @napi-rs/canvas path (D-19).
</verification>

<success_criteria>
- Native text PDFs route native; invisible-overlay and bitmap-heavy PDFs route image-only (PARSE-01, D-20).
- unpdf extracts text; pdfjs+canvas renders an image-only PDF page (D-19).
- HEIC decodes before sharp; photos are oriented and downscaled with a bomb guard (PARSE-02, D-07).
</success_criteria>

<output>
Create `.planning/phases/03-ai-client-and-parse-pipeline/03-04-SUMMARY.md` when done.
</output>
