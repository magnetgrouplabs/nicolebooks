# Phase 3 — Deferred Items

Out-of-scope discoveries logged during execution. Not fixed in the owning plan; tracked here.

| # | Found in | Item | Why deferred | Suggested owner |
|---|----------|------|--------------|-----------------|
| 2 | 03-02 Task 2 | **CLOSED 2026-07-27 by quick task 260727-fb9** (fix `8aaddb0`, handler-level regression pin `fdf9eaf`, e2e invocation proof `6c26cc0`). Original report: **BLOCKER (Phase 2 regression, app-breaking).** `ingestion:scan` always rejects. `src/main/ipc/ingestion.ts:41` runs `ScanRequestSchema.parse(raw)`, but the preload invokes the channel with no argument (`src/preload/index.ts:40`), so `raw` is `undefined` and `z.object({}).strict().parse(undefined)` throws `Invalid input: expected object, received undefined`. Confirmed on the running app with a throwaway Playwright probe: `window.api.ingestion.scan()` REJECTED, so the Bills "Scan now" button can never succeed. **Fix is one character: `ScanRequestSchema.parse(raw ?? {})`** — this preserves the strict-empty gate (any real payload still throws) while accepting the genuine no-arg call. Plan 03-02 applied exactly that normalization to its own `ai:test-connection` / `ai:list-models` handlers, and the same probe confirmed both now resolve. | `src/main/ipc/ingestion.ts` is not in 03-02's `files_modified` and the bug predates this plan (Phase 2, plan 02-01), so fixing it here would be an out-of-scope edit to another phase's file. No unit or e2e spec covers `ingestion.scan()`, which is why Phase 2 shipped green. | DONE — quick task 260727-fb9 applied exactly this: `parse(raw ?? {})` in `src/main/ipc/ingestion.ts`, plus `test/ingestion-ipc-scan.test.ts` (the reject half, unreachable from the renderer) and `e2e/ingestion-scan.spec.ts` (an e2e case that actually invokes `window.api.ingestion.scan()`). **STANDING NOTE, still open for 03-07: any future payload-free handler (the parse channels included) must use `parse(raw ?? {})`.** |
| 3 | 03-04 Task 2 | **CLOSED 2026-07-27 by plan 03-07.** `src/main/parse/pipeline.ts` now runs a pre-decode pixel budget for `.heic`/`.heif` inputs before `prepImage` is called: the declared canvas is read from the ISOBMFF `ispe` box (validated as a real 20-byte FullBox, version 0, so stray bytes spelling `ispe` cannot refuse a legitimate photo) and compared against the same `MAX_INPUT_PIXELS` ceiling sharp uses. Over budget is a `parse-failed` row with plain copy, never an exception through the batch (D-15). An unreadable or absent `ispe` ALLOWS the file, matching Phase 2's inconclusive-detection rule (load on total detection failure, skip only on positive evidence) — and heic-convert needs the same box to decode anyway. Two specs in `test/parse-pipeline.test.ts` pin it: a declared 60000x60000 canvas fails while the rest of the batch still parses, and a 4032x3024 iPhone frame passes. Original report: **Residual DoS gap in T-03-03: the HEIC decode is not pixel-bounded.** `prepImage`'s decompression-bomb guard is `sharp(input, { limitInputPixels: 100_000_000 })`, exactly as the plan's threat register specifies — but for a `.heic`/`.heif` file the bytes pass through `heic-convert` FIRST (they must; sharp's libvips cannot decode HEIC), and heic-convert exposes no pixel or memory cap. A hostile HEIC declaring an enormous canvas is therefore fully decoded before sharp's guard ever applies. JPEG/PNG/image-only-PDF inputs are unaffected. | A real fix needs a design decision this plan does not own: what the cap is (byte size is a weak proxy — a bomb is small by definition), and what happens when it trips. "Flag-and-keep as a parse failure" (D-15) is the right answer and that machinery lives in `parse/pipeline.ts`, not in a leaf transform. Expanding `prepImage`'s contract unilaterally would also change a signature 03-07 is about to consume. | 03-07 (pipeline), as part of wiring per-file isolation: probe HEIC dimensions or bound decode input, and surface a trip as a `parse-failed` row rather than an exception |
| 4 | 03-06 (state update) | **PARSE-04 is complete in code but still shows Pending in `REQUIREMENTS.md`.** Plan 03-03 shipped `validate.ts` + `confidence.ts` and its SUMMARY records `requirements-completed: [PARSE-04]`, but the checkbox and the traceability row were never flipped (`gsd-tools requirements mark-complete PARSE-04` was not run). 03-06 flipped only its own PARSE-05. | Editing another plan's requirement status from this plan would hide a process gap behind a silent fix, and the same omission may exist for other phases' requirements — it wants one deliberate reconciliation pass, not a drive-by. | `/gsd:verify-work 03`, or a `/gsd:quick` running `gsd-tools requirements mark-complete PARSE-04` after confirming 03-03's tests are green |
| 1 | 03-01 Task 1 | `npm run typecheck` (`tsc --build`) does not cover `test/` or `e2e/`. `tsconfig.node.json` includes only `electron.vite.config.ts` + `src/main`, `src/preload`, `src/shared`; `tsconfig.web.json` includes only `src/renderer/src`. So a type error in a spec or in `test/helpers/*` is invisible to the typecheck gate (vitest strips types without checking them). | Pre-existing since Phase 1, unrelated to this plan's changes. Adding a third project reference for `test/` + `e2e/` needs `types: ["node", "vitest/globals"]` and a Playwright `Window.api` declaration — a build-config change with blast radius beyond a foundation plan. `test/helpers/fake-openai-client.ts` was instead verified with a standalone strict `tsc --noEmit` run (exit 0). | A dedicated tooling/quality plan, or Phase 8 packaging hardening |
