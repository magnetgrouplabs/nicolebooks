---
phase: 03-ai-client-and-parse-pipeline
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/main/parse/prompt.ts
  - src/main/parse/extract-fields.ts
  - test/parse-extract.test.ts
autonomous: true
requirements: [PARSE-03]
must_haves:
  truths:
    - "The vision call builds a text-before-image content array: reference transcription first, then the page image(s) declared as ground truth"
    - "Only vendor and total are required in the schema; every other field is nullable ('return null if absent, never invent')"
    - "The structured-output fallback ladder runs strict json_schema -> json_object+schema-in-prompt -> plain-prompt JSON, and the returned text is ALWAYS re-validated with the local BillSchema"
    - "A Zod-invalid response triggers exactly one repair retry, then flag-and-keep as a parse failure"
  artifacts:
    - path: "src/main/parse/prompt.ts"
      provides: "the D-23 prompt as one diffable/testable constant"
      exports: ["BILL_SYSTEM_PROMPT", "buildUserContent"]
    - path: "src/main/parse/extract-fields.ts"
      provides: "vision call + fallback ladder + local BillSchema re-validation + one repair retry (D-25)"
      exports: ["extractFields"]
  key_links:
    - from: "src/main/parse/extract-fields.ts"
      to: "src/shared/schemas.ts BillSchema"
      via: "local Zod re-validation is authoritative over the wire shape"
      pattern: "BillSchema\\.(parse|safeParse)"
---

<objective>
Build the vision extraction step: the D-23 prompt (kept as one testable constant) and the extract-fields module that assembles a text-before-image content array, calls the configured model through the D-25 structured-output fallback ladder, and re-validates the response with the authoritative local BillSchema (one repair retry, then flag-and-keep). Satisfies PARSE-03.

Purpose: This is the only genuinely novel (no in-repo analog) behavior in the phase. The guardrails — required-field minimization, "image is ground truth", "return null if absent" — are what prevent hallucinated invoice numbers and mis-read totals.
Output: src/main/parse/prompt.ts and src/main/parse/extract-fields.ts, driven by the shared fake client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** non-technical bookkeeper, **I want to** configure a vision AI model once and have the app read each scanned bill into validated vendor/date/amount fields with confidence flags, **so that** I can review accurate entries instead of hand-typing every bill.

This plan delivers the "extract structured fields via the configured vision model" step (success criterion 4, extraction half).

<context>
@.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md
@src/shared/schemas.ts
@src/main/ingestion/scan.ts
@test/helpers/fake-openai-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): spec content ordering, required-field minimization, fallback ladder, and repair retry</name>
  <read_first>
    - test/helpers/fake-openai-client.ts (the double: chat.completions.parse/create canned/throwing responses + `.calls` recorder)
    - src/shared/schemas.ts (BillSchema — vendor+total required, rest nullable)
    - 03-CONTEXT.md D-23 (prompt: text-before-image; image ground truth; text noisy reference; return null if absent; only vendor+total non-null-required; money as raw printed string) and D-25 (fallback ladder + one repair retry -> flag-and-keep; second-pass call is orchestrated by the pipeline, not here)
    - 03-RESEARCH.md Directive 4 (system message + user content array lines 347-365), Directive 6 (fallback ladder lines 444-453), Code Example lines 522-546
    - 03-VALIDATION.md rows PARSE-03/D-23 and PARSE-03/D-25
  </read_first>
  <files>test/parse-extract.test.ts</files>
  <action>
    test/parse-extract.test.ts drives extractFields with the shared fake client:
    (a) content shape: assert the user message content array is text-before-image — the reference-transcription text part(s) precede the image_url part(s); when no PDF text is available the transcription part reads a "none"/empty marker; each page image is a separate image_url part.
    (b) required-field minimization: a fake response with `vendor` and `total` present but all other fields null passes local BillSchema; a fake response MISSING `total` fails BillSchema (proving total is required and everything else is genuinely optional).
    (c) fallback ladder: when the fake client is configured to reject a strict `json_schema` request (unsupported), extractFields falls back to `json_object`+schema-in-prompt, then plain-prompt JSON, and STILL parses the returned text through BillSchema (authoritative).
    (d) repair retry: a first response that is Zod-invalid triggers exactly ONE repair retry (assert `.calls` shows two extraction calls with the repair instruction on the second); a still-invalid second response makes extractFields signal a parse failure (returned as a failure marker, not a throw) so the pipeline can flag-and-keep (D-25/D-15).
  </action>
  <verify>
    <automated>npx vitest run test/parse-extract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The spec FAILS to import src/main/parse/extract-fields.ts before Task 2 (RED).
    - Cases assert content ordering, the vendor+total-required minimization, the three-rung ladder, and the single repair retry then failure marker.
  </acceptance_criteria>
  <done>The extraction spec exists and is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): prompt.ts (D-23 constant) + extract-fields.ts (ladder + re-validate + repair)</name>
  <read_first>
    - 03-RESEARCH.md Directive 4 (adopt the recommended prompt VERBATIM — system message lines 347-356 + user content array lines 358-363) and Directive 6 fallback ladder (lines 444-453); Code Example lines 522-546 (zodResponseFormat + local BillSchema.parse)
    - src/shared/schemas.ts (BillSchema import)
    - src/main/ingestion/scan.ts (injectable-deps convention; `client` is ParseDeps.client, injected fake in tests)
    - 03-PATTERNS.md sections "src/main/parse/prompt.ts" and "src/main/parse/extract-fields.ts" (this is the no-analog file — use RESEARCH lines 522-546)
    - 03-CONTEXT.md D-23, D-25
  </read_first>
  <files>src/main/parse/prompt.ts, src/main/parse/extract-fields.ts</files>
  <action>
    prompt.ts: export `BILL_SYSTEM_PROMPT` as the D-23 system message VERBATIM (RESEARCH lines 347-356: extract only present data; return null if absent; image is ground truth, transcription is a noisy reference; report amounts exactly as printed; report dates exactly as printed). Export `buildUserContent({ referenceText, imageDataUrls })` returning the content array in the D-23 order: a `{ type:'text' }` reference-transcription part (or a "none" marker when empty), a `{ type:'text' }` extract-instruction part, then one `{ type:'image_url', image_url:{ url, detail:'high' } }` per page. Keep it one diffable constant/module; wording tunable.
    extract-fields.ts: export `extractFields(deps)` taking an injectable `client` (default: the real openai client from 03-02 ai/client.ts). Build the messages (system = BILL_SYSTEM_PROMPT, user = buildUserContent) at `temperature: 0`. Run the D-25 ladder: rung 1 strict json_schema via `zodResponseFormat(BillSchema, 'bill')` + `client.chat.completions.parse`; on an unsupported-parameter error fall to rung 2 `response_format:{type:'json_object'}` + schema described in the prompt via `client.chat.completions.create`; on failure fall to rung 3 plain-prompt JSON. In EVERY case parse the returned text with the local `BillSchema` (authoritative — RESEARCH Pattern 3). On a Zod failure, do ONE repair retry (append "your last output failed validation: &lt;error&gt;; return corrected JSON only"); if still invalid, return a structured failure marker `{ ok:false, reason }` (NOT a throw) so the pipeline flags-and-keeps (D-25/D-15). Return `{ ok:true, bill }` (raw BillSchema shape; cents coercion is 03-03's validate.ts). Never log the API key or full raw response at error level.
  </action>
  <behavior>
    - user content is text-before-image; empty transcription becomes a "none" marker
    - BillSchema rejects a response missing total; accepts vendor+total with the rest null
    - the ladder degrades strict -> json_object -> plain and always re-validates locally
    - exactly one repair retry on Zod failure, then a failure marker (no throw)
  </behavior>
  <verify>
    <automated>npx vitest run test/parse-extract.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test/parse-extract.test.ts PASSES (GREEN).
    - `grep -n "temperature: 0" src/main/parse/extract-fields.ts` present (deterministic extraction).
    - `grep -n "BillSchema" src/main/parse/extract-fields.ts` shows the local re-validation is applied on every rung.
    - prompt.ts contains the verbatim D-23 phrases "return null" and "ground truth".
  </acceptance_criteria>
  <done>The prompt constant and the vision extraction ladder are green under the fake client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bill content -> model prompt | Bill text/imagery (attacker-influenceable) becomes model input; the model's reply is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering (prompt injection) | extract-fields.ts | mitigate | The model reply is only a candidate — the authoritative local BillSchema re-validation runs on every rung, and the deterministic Zod/arithmetic gate (03-03) is the final authority; the model is never asked to compute money (raw printed strings only, D-23) |
| T-03-01 | Information Disclosure | extract-fields.ts error paths | mitigate | The API key lives in the injected client; extract-fields never logs the key or dumps the full raw response at error level |
</threat_model>

<verification>
- `npx vitest run test/parse-extract.test.ts` green.
- `npm run typecheck` green.
</verification>

<success_criteria>
- Text-before-image content with image as ground truth; only vendor+total required (PARSE-03, D-23).
- Fallback ladder always ends in a local BillSchema re-validation; one repair retry then flag-and-keep (D-25).
</success_criteria>

<output>
Create `.planning/phases/03-ai-client-and-parse-pipeline/03-05-SUMMARY.md` when done.
</output>
