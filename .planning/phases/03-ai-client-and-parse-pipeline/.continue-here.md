---
context: phase
phase: 03-ai-client-and-parse-pipeline
task: 0
total_tasks: 7
status: planned_ready_to_execute
last_updated: 2026-07-24T20:46:51.366Z
---

# Resume: Phase 3 — AI Client and Parse Pipeline

Paused at a **clean boundary**: planning is COMPLETE and plan-checker VERIFIED. Nothing has been executed. The next step is `/gsd:execute-phase 3`.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| image-only PDF -> sharp/prepImage | The first plan-checker pass caught that scanned/image-only PDFs were being fed to `prepImage` (sharp), which cannot decode PDF bytes. Fixed in 03-07: image-only PDFs must go through `renderPdfPageImage` (pdfjs + @napi-rs/canvas); only raw photos (JPEG/PNG/HEIC) go to `prepImage`. | advisory | Implement 03-07 Task 2's source-type branch exactly as written; do NOT collapse the image-only route back to a single prepImage call. Task 1 drives the real `test/fixtures/image-only.pdf` end-to-end to prove it. |
| gsd-sdk invocation | `gsd-sdk query <verb>` is broken on this machine (stale npx cache). | advisory | Use `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" <command> [args]` instead (e.g. `init execute-phase 3`, `commit --files ...`). See project memory `gsd-tooling`. Note the local tool lacks some newer verbs (e.g. `check decision-coverage-plan`). |

<current_state>
Phase 3 is fully planned: 7 plans in 3 waves, all committed, plan-checker passed on revision iteration 2. STATE.md status = Ready to execute. Working tree is clean. All 9 requirements (AI-01..04, PARSE-01..05) and all 26 decisions (D-01..D-26) are covered by concrete tasks.
</current_state>

<completed_work>

- Research (`03-RESEARCH.md`) — stack verified, 6 open directives worked into option menus (commit 0905199)
- Anthony's picks locked as D-19..D-26 in `03-CONTEXT.md` (commit 07a5e3e)
- Pattern map (`03-PATTERNS.md`) — 22 files mapped to in-repo analogs (commit 3905749)
- Validation strategy (`03-VALIDATION.md`) — Nyquist requirement->test map (commit dc65a64)
- 7 plans, 3 waves (`03-01..03-07-PLAN.md`) (commit 2cf0ad5)
- Plan-checker revision: fixed the image-only-PDF blocker + 3 warnings (commit e3b3d7c)
- Re-verification PASSED; STATE marked planned (commit fe3f1d6)
</completed_work>

<remaining_work>

- **Wave 1** — 03-01 foundation: install `openai@6.48.0 unpdf@1.6.2 pdfjs-dist@6.1.200 sharp@0.35.3 heic-convert@2.1.0 @napi-rs/canvas@1.0.2`; define the whole `ai:*`/`parse:*` IPC contract + Zod schemas + preload bridge; shared fake `OpenAIClientLike`
- **Wave 2** (5-way parallel) — 03-02 AI-config, 03-03 validate+confidence, 03-04 route+prep, 03-05 vision extraction, 03-06 `migration0003` cache
- **Wave 3** — 03-07 pipeline integration (auto-parse, per-file isolation + `parsing N/M`, cache-hit-no-recall proof, Bills parse-status surface)
- Then `/gsd:verify-work 3` + the end-of-phase human gate (live parse with a real key)
</remaining_work>

<decisions_made>

- **D-19**: add `@napi-rs/canvas@1.0.2` (only turnkey way to render image-only PDFs, D-07)
- **D-20**: Docling-style layered native-vs-scan gate (0.75 bitmap / 0.90 invisible-OCR / 50 chars+embedded font / 50% pages)
- **D-21**: single multi-image vision call, cap 10 pages (over cap: pages 1-3 + last 2, `truncated` flag)
- **D-22**: second-pass agreement check, image-only docs only, both calls temp 0
- **D-23**: adopt the researcher's recommended prompt verbatim (`parse/prompt.ts`)
- **D-24**: `parsed_results` STRICT cache keyed on file_hash; JSON `field_confidence`; store `raw_response`; `truncated` 0/1 column; `schema_version`
- **D-25**: `maxRetries: 3`, `timeout: 120000`; fallback ladder strict-json-schema -> json_object -> plain-prompt; local Zod authoritative + one repair retry
- **D-26**: separate `parse:parse-batch` channel + `parse:progress` broadcast (mirrors `theme:changed`)
- **Version pins**: honored CLAUDE.md's `openai@6.48.0` / `unpdf@1.6.2` over the newer minors (Anthony's call; revisable)
- **Skipped UI-SPEC and AI-SPEC** — plan directly, reuse Phase 1 branded components (Anthony chose "plan now")
</decisions_made>

<blockers>
- None blocking execution start. The deterministic layers build/test with injected fakes and need no API key.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md` — the 26 locked decisions (D-01..D-26); D-19..D-26 are the "Locked Research Picks" block, authoritative
2. `.planning/phases/03-ai-client-and-parse-pipeline/03-01-PLAN.md` .. `03-07-PLAN.md` — the plans to execute, in wave order
3. `.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md` — the in-repo analog for each new/modified file
4. `.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md` — the Nyquist test contract (each plan opens with a Wave-0 RED task)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: feeding image-only PDF bytes to sharp/`prepImage` -> route them through `renderPdfPageImage` (pdfjs + @napi-rs/canvas) first (D-07/D-19)
- [ANTI-PATTERN]: using `gsd-sdk query ...` -> use `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs"` instead

## Infrastructure State
- Git tree: clean; latest commit `3905749` (all 8 planning commits landed 0905199..fe3f1d6 + PATTERNS)
- No background processes/servers running
- New npm deps are NOT yet installed — 03-01 Task 3 installs them (they don't exist in node_modules until Wave 1 runs)
- Live AI endpoint: none configured; not needed until the end-of-phase human gate

<context>
Planning went smoothly. The only substantive event was the plan-checker catching a genuine correctness blocker (image-only PDFs -> sharp) that would have made scanned/faxed PDF bills unparseable; it's fixed and re-verified. Anthony steered every open research decision himself (his research-then-choose preference) — the 4 load-bearing ones via AskUserQuestion, the rest via researcher-recommended defaults he was told about. The plans are MVP vertical slices, so Wave 1 is a thin foundation and each later slice is independently demoable.
</context>

<next_action>
Start with: `/clear`, then `/gsd:execute-phase 3` (begins Wave 1 / 03-01). Optionally `/gsd:review --phase 3 --all` first for an external peer review of the plans before executing.
</next_action>
