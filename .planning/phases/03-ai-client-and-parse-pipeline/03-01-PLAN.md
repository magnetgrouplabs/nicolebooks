---
phase: 03-ai-client-and-parse-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ipc-contract.ts
  - src/shared/schemas.ts
  - src/preload/index.ts
  - package.json
  - package-lock.json
  - test/ipc-contract.test.ts
  - test/helpers/fake-openai-client.ts
  - e2e/ipc-boundary.spec.ts
autonomous: true
requirements: [AI-02, PARSE-03, PARSE-05]
must_haves:
  truths:
    - "The renderer surface window.api exposes an `ai` group and a `parse` group in addition to settings/secrets/theme/ingestion"
    - "Every ai:* and parse:* channel name is a pinned constant that the channel-stability test asserts"
    - "The AI key and base URL never appear in any IPC payload schema (only the non-secret model id and result objects cross the boundary)"
    - "The six Phase 3 libraries install and load under the Electron ABI"
  artifacts:
    - path: "src/shared/ipc-contract.ts"
      provides: "ai:* + parse:* channel constants, ModelInfo/ParsedFields/ParseFileStatus/ParseProgress/ParseResult types, AiApi/ParseApi interfaces"
      contains: "aiTestConnection"
    - path: "src/shared/schemas.ts"
      provides: "AiTestConnectionSchema, AiSetModelSchema, ParseBatchSchema, ReparseSchema, BillSchema"
      contains: "ParseBatchSchema"
    - path: "src/preload/index.ts"
      provides: "window.api.ai + window.api.parse bridge methods incl. parse.onProgress subscribe/unsubscribe"
      contains: "onProgress"
    - path: "test/helpers/fake-openai-client.ts"
      provides: "shared OpenAIClientLike test double (records calls; canned schema-valid / throwing responses)"
      contains: "OpenAIClientLike"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/shared/ipc-contract.ts"
      via: "Channels constants + Api type"
      pattern: "Channels\\.(ai|parse)"
---

<objective>
Establish the shared foundation every other Phase 3 plan builds on: install the six locked libraries, define the entire ai:* and parse:* IPC contract (channel constants, cross-boundary types, and Zod payload schemas) up front so downstream slices consume fixed contracts without touching shared files, wire the preload bridge, and create the shared fake OpenAI client test double.

Purpose: Interface-first ordering. By owning all contract/schema/preload additions here, Wave 2 slices (AI-config, validation, routing, vision, persistence) run in parallel with zero shared-file conflicts, and the pipeline integration in Wave 3 only adds handler wiring.
Output: Updated ipc-contract.ts / schemas.ts / preload/index.ts, the installed dependencies, the shared `OpenAIClientLike` double, and extended boundary tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** non-technical bookkeeper, **I want to** configure a vision AI model once and have the app read each scanned bill into validated vendor/date/amount fields with confidence flags, **so that** I can review accurate entries instead of hand-typing every bill.

_(Derived faithfully from the ROADMAP Phase 3 goal + its five success criteria + the NicoleBooks core value; the single end user is Nicole per CLAUDE.md.)_

<context>
@.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md
@src/shared/ipc-contract.ts
@src/shared/schemas.ts
@src/preload/index.ts
@test/ipc-contract.test.ts
@e2e/ipc-boundary.spec.ts

<interfaces>
<!-- Executors: these are the fixed contracts this plan creates. Downstream plans import them; they do NOT edit this file again. -->

Channels to add (string constants, grouped with comments; mirror the existing settings/secrets/theme/ingestion style in ipc-contract.ts lines 18-31):
  aiTestConnection: 'ai:test-connection'
  aiListModels: 'ai:list-models'
  aiSetModel: 'ai:set-model'
  parseBatch: 'parse:parse-batch'
  parseReparse: 'parse:reparse'
  parseProgress: 'parse:progress'   (main->renderer broadcast; mirrors themeChanged)

Types to add (mirror ScanFile/ScanResult at lines 84-99, IngestionApi at 107-111):
  ModelInfo { id: string; label?: string; vision: 'vision' | 'vision-family' | 'unknown' }  (rich OpenRouter fields optional)
  ParsedFields { vendor, invoiceNumber, invoiceDate, dueDate, subtotalCents, taxCents, totalCents, currency, suggestedCategory } (nullable optionals except vendor/totalCents)
  FieldConfidence = Record<string, 'high' | 'low' | 'flagged'>
  ParseFileStatus = 'parsed' | 'parse-failed' | 'cached'
  ParseFileResult { filename; hash; status: ParseFileStatus; fields?: ParsedFields; confidence?: FieldConfidence; validationFlags?: string[]; truncated?: boolean }
  ParseProgress { done: number; total: number; filename: string; status: ParseFileStatus }
  ParseBatchResult { files: ParseFileResult[]; summary: { total: number; parsed: number; failed: number; cached: number } }
  AiApi { testConnection(): Promise<{ ok: boolean; models?: ModelInfo[] }>; listModels(): Promise<ModelInfo[]>; setModel(id: string): Promise<boolean> }
  ParseApi { parseBatch(files: {filename:string;hash:string;batchEntryDate:string}[]): Promise<ParseBatchResult>; reparse(fileHash: string): Promise<ParseFileResult>; onProgress(cb:(p:ParseProgress)=>void): () => void }
  Api gains: ai: AiApi; parse: ParseApi
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): pin the new channels and create the shared fake OpenAI client double</name>
  <read_first>
    - test/ipc-contract.test.ts (the whole file — the "Channels are stable strings" block at lines 114-130 is what you extend)
    - src/shared/ipc-contract.ts (existing Channels object, lines 18-31, to mirror the naming style)
    - e2e/ipc-boundary.spec.ts (lines 38-50 — the api-shape assertion to extend)
    - 03-PATTERNS.md Shared Pattern B (the ScanDeps injectable-client convention the fake double serves) and the "src/main/parse/extract-fields.ts" + "src/main/ai/models.ts" assignments (what the double must be able to return)
    - 03-RESEARCH.md Code Example lines 522-546 (the shape extract-fields expects from client.chat.completions.parse) and Directive 6 (models.list() shapes for OpenAI vs OpenRouter)
  </read_first>
  <files>test/ipc-contract.test.ts, e2e/ipc-boundary.spec.ts, test/helpers/fake-openai-client.ts</files>
  <action>
    Extend the `Channels are stable strings` assertion in test/ipc-contract.test.ts to include the six new keys with their exact string values: `aiTestConnection: 'ai:test-connection'`, `aiListModels: 'ai:list-models'`, `aiSetModel: 'ai:set-model'`, `parseBatch: 'parse:parse-batch'`, `parseReparse: 'parse:reparse'`, `parseProgress: 'parse:progress'`. This is RED until Task 2 adds the constants.
    Extend e2e/ipc-boundary.spec.ts api-shape block: `apiShape.top` must now equal `['ai','ingestion','parse','secrets','settings','theme']`; add `ai: Object.keys(window.api.ai).sort()` expecting `['listModels','setModel','testConnection']` and `parse: Object.keys(window.api.parse).sort()` expecting `['onProgress','parseBatch','reparse']`. (e2e runs at the phase gate, not per task.)
    Create test/helpers/fake-openai-client.ts exporting a hand-written `OpenAIClientLike` type plus a `makeFakeClient(opts)` factory — NO `openai` import, so it stays a pure test double. It must: (a) expose `models.list()` returning a caller-supplied array (support both the OpenAI-minimal `{id,object,created,owned_by}` shape and the OpenRouter-rich `{id, architecture:{input_modalities:[...]}, supported_parameters:[...]}` shape); (b) expose `chat.completions.parse(args)` and `chat.completions.create(args)` returning a caller-supplied canned response or throwing a caller-supplied error; (c) record every call (method name + args) on a `.calls` array so tests can assert "client NEVER called" and inspect the message content ordering. Include a `neverCalled` convenience the pipeline no-recall test uses.
  </action>
  <verify>
    <automated>npx vitest run test/ipc-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/ipc-contract.test.ts` FAILS on the Channels assertion before Task 2 (RED), proving the new pins are asserted.
    - test/helpers/fake-openai-client.ts compiles with `npm run typecheck` and imports no `openai` package.
    - The double records calls in a `.calls` array and can be constructed to throw for a named file (used by the D-15 isolation test later).
  </acceptance_criteria>
  <done>New channels are asserted (RED), and the shared fake client double exists and is import-clean.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): add ai/parse channels, cross-boundary types, Zod schemas, and preload bridge</name>
  <read_first>
    - src/shared/ipc-contract.ts (full file — you append to Channels, add types after ScanResult, extend Api)
    - src/shared/schemas.ts (full file — mirror the SettingsSetSchema/ScanRequestSchema bounds discipline, lines 16-37)
    - src/preload/index.ts (full file — mirror the theme.onChange subscribe bridge at lines 29-36 for parse.onProgress, and the ingestion invoke methods at 37-41)
    - 03-PATTERNS.md sections "src/shared/ipc-contract.ts", "src/shared/schemas.ts" (Shared Pattern D), and "src/preload/index.ts" — the exact analog line references
    - 03-CONTEXT.md D-05 (key+baseURL are secrets, never an IPC payload), D-24 (parsed_results field set the ParsedFields type mirrors), D-26 (parse:progress broadcast)
    - 03-RESEARCH.md Directive 4 schema (lines 359-365, the BillSchema field set) and Directive 6 (ModelInfo optional rich fields)
  </read_first>
  <files>src/shared/ipc-contract.ts, src/shared/schemas.ts, src/preload/index.ts</files>
  <action>
    In ipc-contract.ts: append the six channel constants from the &lt;interfaces&gt; block to `Channels` (types + string constants ONLY; keep the zero-runtime-import rule). Add the `ModelInfo`, `ParsedFields`, `FieldConfidence`, `ParseFileStatus`, `ParseFileResult`, `ParseProgress`, `ParseBatchResult` types and the `AiApi` / `ParseApi` interfaces exactly as specified in &lt;interfaces&gt;. Money fields are `subtotalCents`/`taxCents` (number|null) and `totalCents` (number); `ParsedFields` mirrors D-24 columns. Append `ai: AiApi` and `parse: ParseApi` to the `Api` interface. Document in a comment that the AI key + base URL are NOT part of any type here (D-05).
    In schemas.ts: add `AiTestConnectionSchema = z.object({}).strict()` (test-connection reads the key server-side; no renderer payload — like ScanRequestSchema); `AiSetModelSchema = z.object({ modelId: z.string().min(1).max(256) })`; `ParseBatchSchema = z.array(z.object({ filename: z.string().min(1).max(1024), hash: z.string().length(64), batchEntryDate: z.string().min(1).max(32) })).max(500)`; `ReparseSchema = z.object({ fileHash: z.string().length(64) })`; and `BillSchema` (the authoritative model-output gate, RESEARCH lines 528-538): vendor `z.string()`, total `z.string()`, all other fields `z.string().nullable()` (invoice_number, invoice_date, due_date, subtotal, tax, currency, suggested_category) — money stays raw printed string here; cents coercion is 03-06's validate.ts.
    In preload/index.ts: add an `ai` object (`testConnection`, `listModels`, `setModel` as thin `ipcRenderer.invoke` on the channel constants) and a `parse` object (`parseBatch`, `reparse` as invoke; `onProgress` copying the theme.onChange listener+removeListener shape verbatim for `Channels.parseProgress`). Never expose `ipcRenderer` or a generic invoke.
  </action>
  <verify>
    <automated>npx vitest run test/ipc-contract.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/ipc-contract.test.ts` PASSES (channels now pinned; GREEN).
    - `npm run typecheck` passes; `window.api.ai` and `window.api.parse` are typed from the shared Api.
    - `grep -n "ai-api-key\|ai-base-url\|apiKey\|baseURL" src/shared/ipc-contract.ts src/shared/schemas.ts` returns nothing — no secret material in the contract or schemas (D-05).
    - BillSchema requires only `vendor` and `total`; every other field is `.nullable()`.
  </acceptance_criteria>
  <done>The full ai/parse contract, schemas, and preload bridge exist; the channel-stability test is green.</done>
</task>

<task type="auto">
  <name>Task 3: install the six Phase 3 libraries and verify Electron-ABI load</name>
  <read_first>
    - package.json (scripts: `rebuild` = electron-rebuild -f -w better-sqlite3; `postinstall` = npm run rebuild)
    - CLAUDE.md Technology Stack + "What NOT to Use" (locked versions and forbidden libs)
    - 03-RESEARCH.md "Standard Stack" table + "Package Legitimacy Audit" (all six packages [OK]) + Assumptions A1 (@napi-rs/canvas N-API is ABI-stable, no electron-rebuild) + D-19 (canvas approved)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Install the exact versions pinned in CLAUDE.md Technology Stack: `npm install openai@6.48.0 unpdf@1.6.2 pdfjs-dist@6.1.200 sharp@0.35.3 heic-convert@2.1.0 @napi-rs/canvas@1.0.2` (openai and unpdf follow the locked CLAUDE.md pins, which supersede the slightly newer RESEARCH Standard Stack numbers — Anthony's decision). sharp is native and better-sqlite3 already ships; run `npm run rebuild` (electron-rebuild for better-sqlite3) and confirm sharp's prebuilt binary and @napi-rs/canvas's N-API binary load. All six are audited [OK] in RESEARCH (millions of weekly downloads, official repos), and @napi-rs/canvas is locked by D-19, so no legitimacy checkpoint is required. Do NOT add types-only shims for packages that ship their own types.
  </action>
  <verify>
    <automated>node -e "require('sharp'); require('@napi-rs/canvas'); require('better-sqlite3'); require('heic-convert'); require('openai'); console.log('all six load OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The node -e require line prints `all six load OK` with exit 0 (sharp + canvas + better-sqlite3 native/N-API binaries resolve).
    - package.json dependencies list openai 6.48.0, unpdf 1.6.2, pdfjs-dist 6.1.200, sharp 0.35.3, heic-convert 2.1.0, @napi-rs/canvas 1.0.2.
    - `npm run typecheck` still passes.
  </acceptance_criteria>
  <done>All six libraries are installed at the locked versions and load under the Electron/N-API ABI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main (IPC) | Untrusted renderer input crosses at every ai:*/parse:* channel; the key must never cross back |
| npm registry -> build | Third-party package bytes enter the build at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ai:*/parse:* IPC contract | mitigate | Contract + schemas carry no key/baseURL fields (D-05); AiTestConnectionSchema is strict-empty so the renderer sends no credential; grep gate in Task 2 acceptance proves absence |
| T-03-SC | Tampering | npm installs (openai/unpdf/pdfjs-dist/sharp/heic-convert/@napi-rs/canvas) | accept | All six [OK] in RESEARCH Package Legitimacy Audit (official repos, high downloads, empty postinstall); @napi-rs/canvas locked by D-19; no [ASSUMED]/[SUS] packages, so no blocking human checkpoint per the package-legitimacy gate |
</threat_model>

<verification>
- `npx vitest run test/ipc-contract.test.ts` green (channels pinned).
- `npm run typecheck` green (contract + preload types resolve).
- The six libraries load under the ABI (Task 3 node -e check).
- e2e/ipc-boundary.spec.ts api-shape edits present (run at phase gate `npm test`).
</verification>

<success_criteria>
- window.api exposes `ai` and `parse` groups; all six channels are pinned constants.
- No secret material exists in ipc-contract.ts or schemas.ts.
- The shared fake OpenAIClientLike double is importable by Wave 2/3 tests.
- The six locked dependencies are installed and load under Electron.
</success_criteria>

<output>
Create `.planning/phases/03-ai-client-and-parse-pipeline/03-01-SUMMARY.md` when done.
</output>
