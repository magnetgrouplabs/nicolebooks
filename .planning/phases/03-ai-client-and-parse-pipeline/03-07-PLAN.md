---
phase: 03-ai-client-and-parse-pipeline
plan: 07
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04, 03-05, 03-06]
files_modified:
  - src/main/parse/pipeline.ts
  - src/main/ipc/parse.ts
  - src/main/ipc/register.ts
  - src/renderer/src/screens/BillsScreen.tsx
  - test/parse-pipeline.test.ts
  - test/parse-cache.test.ts
autonomous: true
requirements: [PARSE-05]
must_haves:
  truths:
    - "After a scan, the renderer fires parse:parse-batch on the loaded set and the pipeline parses each file: cache lookup -> route -> extract/prep -> vision -> validate+confidence -> cache write"
    - "A cache hit returns the stored row and the injected model client is NEVER called (PARSE-05 no-recall)"
    - "One file failing (throw / rate limit / unreadable) becomes a parse-failed retry row while the rest of the batch proceeds (per-file isolation, D-15)"
    - "Progress streams via a parse:progress broadcast as 'parsing N/M'; the Bills screen shows per-file parsed/parse-failed/cached status"
    - "Image-only docs get the D-22 second-pass agreement call; native PDFs skip it"
  artifacts:
    - path: "src/main/parse/pipeline.ts"
      provides: "parseBatch orchestrator with per-file isolation, cache-first, progress emit (D-13/D-15/D-22)"
      exports: ["parseBatch"]
    - path: "src/main/ipc/parse.ts"
      provides: "parse:parse-batch / parse:reparse handlers + parse:progress webContents.send broadcast (D-26)"
      exports: ["registerParseIpc"]
    - path: "src/renderer/src/screens/BillsScreen.tsx"
      provides: "auto-parse trigger + parse-status badges + 'parsing N/M' indicator + retry affordance (D-18b)"
      contains: "parseBatch"
  key_links:
    - from: "src/renderer/src/screens/BillsScreen.tsx"
      to: "parse:parse-batch"
      via: "window.api.parse.parseBatch(loadedFiles) after scan; onProgress subscribe"
      pattern: "window\\.api\\.parse\\.parseBatch"
    - from: "src/main/parse/pipeline.ts"
      to: "src/main/parse/cache.ts"
      via: "cache-first getCached before any model call; putCached last"
      pattern: "getCached|putCached"
---

<objective>
Wire the whole parse pipeline together and light up the end-to-end demo: the renderer auto-fires parse:parse-batch on the loaded set after a scan; the main-side orchestrator runs each file through cache lookup -> route -> extract/prep -> vision -> validate+confidence -> cache write with per-file isolation; progress streams back as "parsing N/M"; and the Bills screen shows per-file parsed/parse-failed/cached status with a retry affordance. This is the slice that makes PARSE-05's no-recall guarantee real and delivers the demoable parse capability.

Purpose: The final vertical slice — after this, a user scans a folder and watches bills parse into fields, and a re-scan of the same bytes never re-charges the model.
Output: src/main/parse/pipeline.ts, src/main/ipc/parse.ts, register.ts wiring, and the Bills parse-status surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** non-technical bookkeeper, **I want to** configure a vision AI model once and have the app read each scanned bill into validated vendor/date/amount fields with confidence flags, **so that** I can review accurate entries instead of hand-typing every bill.

This plan lights up the end-to-end parse: scan a folder, watch each bill parse into validated fields, and never re-charge on a re-scan.

<context>
@.planning/phases/03-ai-client-and-parse-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-RESEARCH.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-PATTERNS.md
@.planning/phases/03-ai-client-and-parse-pipeline/03-VALIDATION.md
@src/main/ingestion/scan.ts
@src/main/ipc/ingestion.ts
@src/main/ipc/theme.ts
@src/main/ipc/register.ts
@src/renderer/src/screens/BillsScreen.tsx
@src/preload/index.ts
@test/helpers/fake-openai-client.ts
@src/shared/ipc-contract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): spec per-file isolation, cache-hit-no-recall, and progress counts</name>
  <read_first>
    - test/helpers/fake-openai-client.ts (the double with `.calls` + `neverCalled` — the no-recall proof spies on it)
    - test/parse-cache.test.ts (created in 03-06 — extend it with the pipeline no-recall case; do not re-create the storage cases)
    - test/ingestion-scan.test.ts (temp DB + migrate lifecycle + injected-deps convention parseBatch mirrors)
    - src/main/ingestion/scan.ts (runScan per-file try/catch loop, lines 110-131 — the D-15 isolation pattern pipeline copies)
    - 03-CONTEXT.md D-13 (auto-parse after scan), D-14 (cache key), D-15 (per-file isolation + progress + retry), D-22 (second-pass image-only only), D-26 (separate parse:parse-batch channel + parse:progress broadcast)
    - 03-VALIDATION.md rows D-15 (parse-pipeline.test.ts) and PARSE-05/D-14 (cache-hit-no-recall)
  </read_first>
  <files>test/parse-pipeline.test.ts, test/parse-cache.test.ts</files>
  <action>
    test/parse-pipeline.test.ts drives `parseBatch(files, deps)` with a migrated temp DB and the shared fake client injected:
    (a) per-file isolation (D-15): a 3-file batch where the fake client throws for file 2 -> file 2 comes back status 'parse-failed', files 1 and 3 'parsed'; the batch does not abort.
    (b) progress: a progress callback receives `{ done, total, filename, status }` events counting 1/3, 2/3, 3/3 in order.
    (c) cache-first / second-pass: for an image-only doc the pipeline makes the D-22 second call; for a native PDF it does not (assert call counts on the fake client).
    Extend test/parse-cache.test.ts: pre-seed parsed_results with a row for a hash, run parseBatch on a file with that same hash, assert the returned status is 'cached', the fields equal the seeded row, and the injected client's `.calls` is empty (model NEVER called — PARSE-05 no-recall).
  </action>
  <verify>
    <automated>npx vitest run test/parse-pipeline.test.ts test/parse-cache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/parse-pipeline.test.ts FAILS to import src/main/parse/pipeline.ts before Task 2 (RED).
    - The no-recall case asserts the injected client is never called on a cache hit.
    - The isolation case proves one failure does not abort the batch.
  </acceptance_criteria>
  <done>The pipeline isolation, no-recall, and progress specs exist and are RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): pipeline.ts orchestrator + parse IPC handlers + progress broadcast</name>
  <read_first>
    - src/main/ingestion/scan.ts (runScan structure + ScanDeps + per-file try/catch — THE template for parseBatch/ParseDeps)
    - src/main/ipc/ingestion.ts (async handler shape) + src/main/ipc/theme.ts (lines 22-27 — the webContents.send broadcast to copy for parse:progress)
    - src/main/ipc/register.ts (add registerParseIpc — note 03-02 already added registerAiIpc here)
    - src/main/parse/cache.ts, route.ts, extract-pdf.ts, prep-image.ts, extract-fields.ts, validate.ts, confidence.ts (the modules pipeline composes — from 03-03/04/05/06)
    - 03-PATTERNS.md sections "src/main/parse/pipeline.ts" (lines 359-363), "src/main/ipc/parse.ts" (lines 203-219), Shared Pattern C
    - 03-CONTEXT.md D-13/D-14/D-15/D-21/D-22/D-25/D-26; 03-RESEARCH.md architecture diagram (steps 1-6) and Directive 2 (page cap 10; over cap send pages 1-3 + last 2 + `truncated` flag)
  </read_first>
  <files>src/main/parse/pipeline.ts, src/main/ipc/parse.ts, src/main/ipc/register.ts</files>
  <action>
    pipeline.ts: export `parseBatch(files, deps: ParseDeps)` mirroring runScan(deps: ScanDeps). ParseDeps = `{ db?, client?, readFile?, now?, onProgress? }` (Shared Pattern B). For each loaded file, inside a per-file try/catch (Shared Pattern C / D-15): (1) `getCached(db, hash)` — a HIT returns status 'cached' WITHOUT any model call (PARSE-05); (2) route the file (routeFile — native vs image-only); (3) native -> extractPdfText + renderPdfPageImage per page (cap 10, D-21: over cap send pages 1-3 + last 2 and set `truncated`), image-only -> prepImage; (4) `extractFields` (D-25 ladder); for image-only docs make the D-22 second call and pass both results to `agreementFlags`; (5) `validateBill` (cents/dates/arithmetic) + `computeConfidence` (+ agreement flags merged); (6) `putCached` the validated row (store route, page_count, model, base_url_host host-only, parsed_at via injected `now`, schema_version). Emit `onProgress({ done, total, filename, status })` after each file. A thrown/failed-extraction file becomes a `parse-failed` result and the loop CONTINUES. Return `ParseBatchResult`.
    parse.ts: export `registerParseIpc()` with `parse:parse-batch` (assertTrustedSender FIRST, ParseBatchSchema.parse, run parseBatch wired to emit progress via `win.webContents.send(Channels.parseProgress, progress)` to the sender window) and `parse:reparse` (assertTrustedSender, ReparseSchema.parse, force a fresh parse of one hash bypassing the cache — D-14 explicit re-parse override). The progress broadcast copies theme.ts's send pattern (main-initiated; no sender to validate on that path).
    register.ts: add `import { registerParseIpc } from './parse'` and call `registerParseIpc()` inside `registerIpc()` (alongside the existing registerAiIpc from 03-02).
  </action>
  <behavior>
    - a cache hit returns 'cached' and never calls the client
    - one failing file yields a 'parse-failed' row; the batch completes
    - progress fires once per file with correct done/total
    - image-only docs trigger the second-pass call; native PDFs do not; over-10-page PDFs set truncated
  </behavior>
  <verify>
    <automated>npx vitest run test/parse-pipeline.test.ts test/parse-cache.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test/parse-pipeline.test.ts and test/parse-cache.test.ts PASS (GREEN), including the no-recall and isolation cases.
    - `grep -n "getCached" src/main/parse/pipeline.ts` shows the cache lookup precedes any extractFields call (cache-first).
    - parse.ts handlers call `assertTrustedSender` as the first statement and Zod-parse before work.
    - register.ts calls both registerAiIpc and registerParseIpc.
  </acceptance_criteria>
  <done>The orchestrator, IPC handlers, and progress broadcast are green with the no-recall guarantee proven.</done>
</task>

<task type="auto">
  <name>Task 3: Bills parse-status surface (auto-parse trigger, per-file badges, 'parsing N/M', retry)</name>
  <read_first>
    - src/renderer/src/screens/BillsScreen.tsx (whole file — extend the loaded-results list; reuse STATUS_VARIANT/STATUS_LABEL maps lines 35-49, the ScanRow component lines 87-115, and the include-anyway control pattern lines 107-111 for the retry affordance)
    - src/preload/index.ts (parse.onProgress subscribe/unsubscribe shape added in 03-01, mirroring theme.onChange)
    - src/shared/ipc-contract.ts (ParseFileStatus, ParseProgress, ParseBatchResult, ParseApi from 03-01)
    - 03-CONTEXT.md D-18b (parse-status/progress surface; reuse branded components), D-13 (auto-parse after scan), D-15 (retry just the failed ones), D-26 (fire parse:parse-batch separately from the scan IPC call)
    - 03-PATTERNS.md Shared Pattern E + section "src/renderer/src/screens/BillsScreen.tsx"
  </read_first>
  <files>src/renderer/src/screens/BillsScreen.tsx</files>
  <action>
    Extend BillsScreen: after a successful `window.api.ingestion.scan()`, fire `window.api.parse.parseBatch(loadedFiles)` on the loaded set (D-26 — a separate call, NOT inside the scan). Subscribe to `window.api.parse.onProgress(cb)` inside a useEffect with an unsubscribe cleanup (same shape as theme.onChange) and render a "parsing N/M" indicator from the progress events while a batch runs. Add per-file parse-status badges (`parsed` -> default/success, `cached` -> secondary "Cached", `parse-failed` -> destructive) reusing the STATUS_VARIANT/STATUS_LABEL maps and the ScanRow component. A `parse-failed` row gets a "Retry" affordance (mirror the include-anyway control) calling `window.api.parse.reparse(fileHash)` for just that file (D-15). All parse state is renderer-local; colors are semantic theme classes (no hardcoded hex). Do NOT build the rich editable review table (Phase 6) — only status + progress.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <human-check>At the end-of-phase human gate (VALIDATION.md Manual-Only rows 1 and 3): with a real key + vision model configured, drop one native PDF and one photo into the inbox, click Scan now; confirm the Bills screen shows a "parsing N/M" indicator, then per-file parsed status and populated fields; a forced-failure file shows a parse-failed row with a Retry control; re-scanning the same files shows 'cached' status and does not re-charge the model.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - Source contains `window.api.parse.parseBatch`, `window.api.parse.onProgress`, and `window.api.parse.reparse`, with an unsubscribe cleanup returned from the onProgress useEffect.
    - `grep -n "#\|rgb(\|hsl(" src/renderer/src/screens/BillsScreen.tsx` shows no hardcoded color literals.
    - parseBatch is fired outside the scan invoke (separate call), per D-26.
  </acceptance_criteria>
  <done>The Bills parse-status surface is complete and typechecks; live behavior is confirmed at the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main (parse channels) | The renderer triggers a batch parse; the main process performs all network/fs/DB work |
| batch loop | One untrusted file's failure must not compromise the batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering (prompt injection) | pipeline.ts | mitigate | The deterministic validate+confidence gate (03-03) runs on every file before caching; model output is never authoritative |
| T-03-02 / T-03-03 | Tampering / DoS (malicious PDF / bomb / one bad file) | pipeline.ts per-file loop | mitigate | Per-file try/catch bounds the blast radius to one parse-failed row (D-15); the 10-page cap (D-21) bounds render/token work; parsing runs post-materialization |
| T-03-01 | Information Disclosure | parse.ts / pipeline.ts | mitigate | Handlers assertTrustedSender + Zod-parse; the API key stays inside the injected client and is never returned to the renderer or logged; progress carries only filename + status |
</threat_model>

<verification>
- `npx vitest run test/parse-pipeline.test.ts test/parse-cache.test.ts` green (isolation + no-recall + progress).
- `npm run typecheck` green.
- Full phase gate: `npm test` (unit + Playwright e2e ipc-boundary) green, plus the end-of-phase live-parse manual smoke.
</verification>

<success_criteria>
- Auto-parse fires after scan; a cache hit never re-calls the model (PARSE-05, D-13/D-14).
- Per-file isolation: one failure is a retry row, the batch continues; "parsing N/M" progress shows (D-15/D-26).
- Image-only docs get the second-pass; native PDFs skip it (D-22); over-cap PDFs are truncated-flagged (D-21).
- The Bills screen shows per-file parsed/parse-failed/cached status with retry.
</success_criteria>

<output>
Create `.planning/phases/03-ai-client-and-parse-pipeline/03-07-SUMMARY.md` when done.
</output>
